---
id: LC-D-O2-COIN-TRANSMISSION
titre: "Front O₂-construction (chantier #2, facteur α) — exécution de la VOIE D'INVENTION du COIN DE TRANSMISSION codim-2 D↔N (fork S-O2C-4) : VERDICT v0.1 = `TC-b` (délimitation, mieux situé ; AUCUN sceau — S-O2C-4-3 non armé, aucune construction finie concrète émergée ; NO-FETCH, fork S-O2C-4-2 = HOLD honoré ⟹ aucun amendement R-7). Exécution contre le gel anti-fit du cadrage LC-WORK-CADRAGE-O2-COIN-TRANSMISSION (sha recouvrable 7cbf1072af03674062b07b585f13cca209179a2d851c71ab02ef341d08edba9f), cibles TC-G1..G4 + TC-R figées AVANT cette algèbre. RÉSULTAT par cible. (TC-G1, forme) AVANCE vs COIN-b : le coin de transmission se DÉCOMPOSE structurellement en S_TC = −(1/8πG)∮_B √σ [η + κ(p)·Φ] d²θ — backbone LMPS η=arcsinh(n·s) (consommé) + correction conforme κ(p)·Φ (poids κ hérité du terme GHY-p de LSW, Φ = pièce conjuguée de la face N) ; point dérivé NO-FETCH : la face D Dirichlet PINCE √σ au coin (B ⊂ D∩N) ⟹ δ√σ=0 sur B même quand la face N est conforme, donc la variation se réduit à √σ·δη (mécanisme d'annulation intact) ; MUR no-fetch : κ(p) et Φ exigent le terme de bord conforme explicite, absent des 5 corps KB ⟹ forme DÉCOMPOSÉE, pas DÉRIVÉE. (TC-G2, finitude z→0) NON franchie no-fetch : contre-terme de coin codim-2 conforme + renormalisation FG au bord conforme absents du KB ; INDÉTERMINÉE — mais désormais ISOLÉE comme le gate. (TC-G3, sort de p, PORTANTE) GATÉ sur TC-G2 : p reste libre (lean, cohérent avec η géométrique du backbone) SAUF si la finitude renormalisée z→0 sélectionne des valeurs de p ⟹ fixation de p ⟺ finitude ⟹ NON tranché no-fetch ; lean = C1-b. (TC-G3★) dans le backbone, p (codim-1, poids LSW) et η (codim-2, géométrique) INDÉPENDANTS ; un lien p=f(η) ne pourrait passer QUE par Φ ⟹ fetch-gated, NON tranché ; lean = indépendants. (TC-G4) AdS-only RESPECTÉ (aucun transport dS tenté ; β≡G3 INTACT). (TC-R) firewall : m1 CONFIRMÉ (coin retiré ⟹ δC_mixte=δη+κ·δΦ≠0, mal-position réapparaît) ; m2 MORD (face N → Dirichlet ⟹ κ→0 ⟹ Ξ→η ⟹ retour exact au η-term nu de LMPS — limite same-type obligatoire tenue, non-tautologique) ; m3 NON testable no-fetch (signe de Φ requis) — vérifications STRUCTURELLES symboliques, AUCUN sceau de continuité (pas de construction finie). VERDICT = TC-b (délimitation, mieux situé) : première DÉCOMPOSITION structurelle du coin de transmission (backbone+correction), m2 pincé, mais κ(p)/Φ/finitude z→0 non KB-dérivables ⟹ PAS de TC-a (G1∧G2∧G3 non toutes satisfaites au sens fort) ⟹ α reste C1-b. APPORT (sans incrément de comptage) : la surface inventive passe de « construire le coin » à un objet RESSERRÉ — fournir κ(p), Φ, et le contre-terme de coin codim-2 conforme z→0 ; fetch resserré (priorité Witten ~1805.11559 → κ(p)/Φ ; Compère-Marolf ~0805.1902 → arête codim-2 ; Jubb et al ~1612.00149 → taxonomie coin mixte) = NOUVEL amendement R-7 daté. SANS SURCLASSEMENT (§6.4) : décomposer la forme ≠ dériver κ(p)/Φ ≠ finir au bord conforme ≠ fixer p ≠ construire α ≠ construire O₂ (β≡G3 reste ouvert) ≠ fermer D1 ; {A4 ; A2★ ; N} INCHANGÉ ; D1 non clos ; N non fixé ; A4 non réduit ; A2★ non tranché ; CCC non démontrée."
codename: LC-RACCORD
tags: [verdict, délimitation, TC-b, O2-construction, chantier-2, facteur-alpha, coin-transmission, coin-mixte-DN, codim-2, terme-de-coin, backbone-LMPS, correction-conforme, boost-eta, parametre-p, LSW, GHY, Neumann, conforme, gate-C1, C1-b, finitude-z0, anomalie-de-coin, fetch-gated, AdS-only, beta-G3, no-fetch, sans-sceau, S-O2C-4, TC-G1, TC-G2, TC-G3, TC-R, falsifiabilite-W3, anti-fit, R-36, R-7, §6.4]
type: "verdict de front (exécution no-fetch de la voie d'invention coin de transmission D↔N, fork S-O2C-4, contre le gel anti-fit 7cbf1072…dba9f). Cibles TC-G1..G4 + TC-R figées AVANT l'algèbre. Algèbre symbolique de backbone + firewall STRUCTURELS (support de raisonnement, NON sceau de continuité — aucune construction finie). Verdict TC-b (délimitation, mieux situé). Fichier-verdict distinct prévu par le cadrage. NO-FETCH (fork S-O2C-4-2 HOLD honoré) ⟹ aucun amendement R-7 ici ; tout fetch des candidats resserrés = nouvel amendement R-7 daté."
statut: "déposé — VERDICT TC-b CONSOLIDÉ MAXIMAL (v0.3, post-fetch A_c). v0.3 : fetch A_c consommé (Gustavsson 1911.04178 ; Odak-Speziale 2109.02883 ; amendement R-7 LC-WORK-AMENDEMENT-R7-O2-COIN-AC 2026-06-23, sha recouvrable 1023a02d…9e51c) contre le MÊME gel 7cbf1072 ⟹ TC-G2 FRANCHIE (finitude renormalisée ∀p : Gustavsson = anomalie conforme INDÉPENDANTE de k ⟹ le coin n'ajoute aucune anomalie p-dépendante ; Odak-Speziale = subtraction symplectique finie ∀b), TC-G3 TRANCHÉE (p≡b LIBRE : bonne-position [ϑ^EH même ∀b] ET finitude tiennent ∀p ⟹ aucun p★ isolé ⟹ C1-b POSITIF), l'anomalie sélectrice de p A_c(p)·log z (hypothèse v0.2 R-48) est RÉFUTÉE, Φ IDENTIFIÉ concrètement = moment ADM sans-trace Π̄_⟨μν⟩ (charge de York O-S éq. 3.17) = conjugué sans-trace de Witten, p≡b (D/York/N = 2/⅔/0). α reste C1-b — désormais classification FINALE du facteur de coin sur l'axe α (famille à un paramètre p, PAS une construction unique) ⟹ AUCUN sceau (S-O2C-4-3 non armé). Firewalls m1/m2 CONCRÉTISÉS via la table b/c d'O-S. (v0.2, post-fetch resserré). v0.1 = TC-b no-fetch (décomposition S_TC=−(1/8πG)∮√σ[η+κ(p)·Φ]). v0.2 : fetch armé (Witten 1805.11559 ; Compère-Marolf 0805.1902 ; Jubb 1612.00149, amendement R-7 2026-06-23) ⟹ Φ et κ(p) IDENTIFIÉS (face N conforme fixe [γ]+K, laisse libre la courbure extrinsèque sans-trace ⟹ résidu codim-2 porté par l échelle δω ; κ(p)=poids York/Compère-Marolf), TC-G1 forme DÉRIVÉE EN STRUCTURE (coefficient exact formalisable) ; TC-G2 finitude CONDITIONNELLE (contre-terme de coin existe ; résidu = anomalie de coin conforme A_c(p)·log z) ; TC-G3 GATÉ sur A_c(p) — la bonne-position seule NE FIXE PAS p (soluble ∀ p), seule la finitude/anomalie le sélecterait ; A_c(p) exige le contre-terme de coin conforme (source NON armée) ⟹ NOUVEAU mur nommé ⟹ p NON fixé EN SCOPE. PAS de TC-a ⟹ AUCUN sceau ⟹ α reste C1-b. Firewalls m1/m2 MORDENT (vérif structurelle EXIT 0). SANS SURCLASSEMENT : {A4 ; A2★ ; N} INCHANGÉ ; D1 non clos ; N non fixé ; CCC non démontrée."
version: 0.3
langue: fr
maj: "2026-06-23 — v0.3 : CONSOLIDATION post-FETCH A_c (sur GO Thierry), contre le MÊME gel anti-fit 7cbf1072...dba9f (cibles TC-G1..G4 + TC-R INCHANGÉES). Fetch exécuté sous amendement R-7 daté LC-WORK-AMENDEMENT-R7-O2-COIN-AC (2026-06-23, sha recouvrable 1023a02d4d3df89a4944127d7de721a1c7eb4a5e71a2eaefe58b5e0af319e51c ; EXTENSION de portée R-49) : sources ARMÉES consommées = Gustavsson ~JHEP 05(2020)064 = 1911.04178 (renormalisation EH + contre-termes de coin ; bords S^k×H^{d−k} ; ANOMALIE CONFORME indépendante de k ; prescription de coin I_ct,corn=−I_corn ; ne pas tronquer la série de contre-termes pour d impair) ; Odak-Speziale ~JHEP 11(2021)224 = 2109.02883 (famille ℓ^b=b·s·K, b=2/⅔/0 = Dirichlet/York/Neumann ; corner Lagrangien ℓ^c=c·β·ε_S, c=2/0/0 ; β=boost normal-normal=η ; potentiel symplectique de coin ϑ^EH MÊME ∀b ; charge renormalisée finie ∀b par subtraction §3.4 ; coins non-orthogonaux). GAINS PAR CIBLE : TC-G1 PLEINEMENT DÉRIVÉE EN STRUCTURE — p≡b (poids GHY retenu, O-S éq. 2.13), η=c·β (Hayward, O-S éq. 2.15/2.16), Φ=Π̄_⟨μν⟩ moment ADM sans-trace (charge de York, O-S éq. 3.17) = conjugué sans-trace de Witten. TC-G2 FRANCHIE (de CONDITIONNELLE à FINIE) : le coin est renormalisable à FINI pour tout p — Gustavsson montre que l'anomalie conforme (coeff. du log) est INDÉPENDANTE de la structure de coin (k) ⟹ le coin n'engendre AUCUNE anomalie conforme p-dépendante ; Odak-Speziale renormalisent la charge incluant le coin à FINI ∀b. TC-G3 TRANCHÉE → p LIBRE : la bonne-position (ϑ^EH identique ∀b, O-S §3.2) ET la finitude (∀b) tiennent pour TOUT p ⟹ AUCUN p★ isolé ⟹ C1-b CONFIRMÉ POSITIVEMENT (plus un lean) ; l'hypothèse v0.2 R-48 d'une anomalie de coin conforme A_c(p)·log z SÉLECTRICE de p est RÉFUTÉE (il n'y a pas de sélecteur ; le choix de p = choix de fermeture du système au sens d'Odak-Speziale, liberté physique réelle, Kerr → M/2M⁄3/M⁄2). TC-G3★ : p (codim-1, poids b) et η (codim-2, boost géométrique β) CONFIRMÉS indépendants (Φ=Π̄_⟨μν⟩ et β=boost sont des données géométriques distinctes au coin). FIREWALLS CONCRÉTISÉS via la table b/c d'O-S : m2 MORD (face N → Dirichlet ⟹ b:p→2, c:0→2 ⟹ le terme de Hayward 2β réapparaît, κ(p)·Φ → 0 ⟹ retour exact au η-term nu) ; m1 MORD (coin retiré ⟹ la variation de coin ∓2δβε_S [O-S éq. 2.6] n'est plus annulée ⟹ mal-position). VERDICT v0.3 = TC-b CONSOLIDÉ MAXIMAL : le gate TC-G2 est FERMÉ (finitude établie ∀p par deux cadres de renormalisation indépendants) et TC-G3 est RÉSOLU (p libre, C1-b positif) ⟹ le facteur de coin est une FAMILLE à un paramètre, PAS une construction unique ⟹ PAS de TC-a ⟹ AUCUN sceau (S-O2C-4-3 non armé) ⟹ PAS d'audit froid (S-O2C-4-4) ⟹ α reste C1-b, désormais classification FINALE sur l'axe coin (la voie d'invention coin de transmission est ÉPUISÉE : elle ne réduit pas α). Registre : R-50 (Odak-Speziale identifie p≡b et η=c·β ; Φ=Π̄_⟨μν⟩=charge de York ; TC-G1 pleinement dérivée en structure) ; R-51 (TC-G2 FRANCHIE : anomalie conforme indép. de k [Gustavsson] + renormalisation finie ∀b [Odak-Speziale] ⟹ aucune anomalie de coin p-dépendante ; R-48 RÉFUTÉE) ; R-52 (TC-G3 TRANCHÉE → p LIBRE : bonne-position et finitude ∀p ⟹ aucun p★ ⟹ C1-b FINAL ; axe coin épuisé ; pivot indiqué = β≡G3 #1 ou A4 #3). Patch ADDITIF (version+statut+maj+§11 ; corps v0.1+v0.2 verbatim). AUCUN sceau, AUCUNE construction finie unique. SANS SURCLASSEMENT (§6.4) : franchir la finitude et trancher p-libre ≠ fixer p ≠ exhiber TC-a ≠ construire α (au contraire : montrer qu'il n'existe pas de α-de-coin unique) ≠ construire O₂ (β≡G3 reste) ≠ fermer D1 ; {A4 ; A2★ ; N} INCHANGÉ ; α reste C1-b ; N non fixé ; A4 non réduit ; A2★ non tranché ; CCC non démontrée. | 2026-06-23 — v0.2 : CONSOLIDATION post-FETCH RESSERRÉ du verdict TC-b, sur GO Thierry, contre le MÊME gel anti-fit 7cbf1072...dba9f (cibles TC-G1..G4 + TC-R INCHANGÉES). Fetch exécuté sous amendement R-7 daté LC-WORK-AMENDEMENT-R7-O2-COIN-TRANSMISSION (2026-06-23, sceau recouvrable 4a3d0a71...fadde) : sources ARMÉES consommées = Witten ~1805.11559 (BC conforme : fixe la classe conforme [γ] + la courbure moyenne K, laisse LIBRE la courbure extrinsèque SANS-TRACE comme conjugué ; problème elliptique/bien posé) ; Compère-Marolf ~0805.1902 (famille de BC à métrique de bord dynamique ; contre-termes rendant les fluctuations normalisables ⟹ finitude z→0 par renormalisation ; famille à paramètre = origine de κ(p)) ; Jubb-Samuel-Sorkin-Surya ~1612.00149 (pullback seul fixé ⟹ terme de coin = angle de boost η, confirme le backbone LMPS face D ; contre-termes de coin requis au bord asymptotique). GAINS PAR CIBLE : TC-G1 passe de DÉCOMPOSÉE à DÉRIVÉE EN STRUCTURE — Φ IDENTIFIÉ = conjugué du résidu d échelle δω de la face N (∝ courbure extrinsèque sans-trace, Witten), κ IDENTIFIÉ = poids de la famille York/Compère-Marolf ; coefficient exact = formalisable (équation de bord explicite). TC-G2 passe d INDÉTERMINÉE à FINITUDE CONDITIONNELLE : l intégrande de coin ~ z^{-2}(η+κΦ)√σ_0 (divergent nu), le contre-terme de coin conforme soustrait z^{-2}/z^0, le résidu est une ANOMALIE DE COIN CONFORME A_c(p)·log z. TC-G3 (sort de p) : la bonne-position δC_mixte=0 DÉTERMINE S_TC comme fonction de p mais NE FIXE PAS p (soluble ∀ p) ; seule la finitude/anomalie sélecterait p ⟹ p fixé ⟺ A_c(p)=0 a une solution isolée (raffine R-44). NOUVEAU MUR : A_c(p) exige le CONTRE-TERME DE COIN CONFORME explicite, qui vient d une source NON armée (renormalisation EH avec contre-termes de coin ~JHEP 05(2020)064 ; Brown-York à BC mixte ~JHEP 11(2021)224, à confirmer) ⟹ p NON fixé EN SCOPE ⟹ C1-b TENU. Firewalls (informés par le fetch) : m2 MORD (face N → Dirichlet ⟹ δω=0 ⟹ κ(p)·Φ → 0 ⟹ retour exact au η-term nu de LMPS) ; m1 MORD (coin retiré ⟹ le résidu d échelle δω réapparaît ⟹ mal-position) ; m3 partiellement adressable (signe de Φ via K̃ de Witten) mais test complet gaté sur A_c. Vérif STRUCTURELLE SymPy verif_O2_coin_transmission_fetch.py EXIT 0 (C1 résidu=δω ; C2 δC_mixte ; C3 p non fixé par bonne-position ; C4 m2 ; C5 m1 ; C6 gate A_c). VERDICT v0.2 = TC-b CONSOLIDÉ (mieux situé) : le fetch a fait AVANCER G1 (dérivée) et G2 (conditionnelle) et a AIGUISÉ le gate G3 (bonne-position ≠ fixation de p ; anomalie = sélecteur), mais N A PAS produit de construction finie (A_c(p) non calculée, contre-terme non armé) ⟹ PAS de TC-a ⟹ AUCUN sceau (S-O2C-4-3 non armé) ⟹ PAS d audit froid (S-O2C-4-4) ⟹ α reste C1-b. Registre : R-46 (face N conforme fixe [γ]+K, laisse K̃ libre ⟹ le résidu codim-2 est l échelle δω, non le boost ⟹ Φ = conjugué de δω ; κ = poids York/CM) ; R-47 (bonne-position δC_mixte=0 détermine S_TC(p) mais NE FIXE PAS p ; sélecteur = finitude) ; R-48 (finitude z→0 : résidu = anomalie de coin conforme A_c(p)·log z ; p fixé ⟺ A_c(p)=0 isolé ; A_c exige le contre-terme de coin conforme, source NON armée ⟹ nouveau mur, nouvel amendement R-7 requis). Patch ADDITIF (version+statut+maj+§10 ; corps v0.1 verbatim). AUCUN sceau, AUCUNE construction finie. SANS SURCLASSEMENT (§6.4) : identifier κ/Φ et nommer l anomalie ≠ calculer A_c(p) ≠ fixer p ≠ exhiber TC-a ≠ construire α ≠ construire O₂ (β≡G3 reste) ≠ fermer D1 ; {A4 ; A2★ ; N} INCHANGÉ ; α reste C1-b ; N non fixé ; A4 non réduit ; A2★ non tranché ; CCC non démontrée. | 2026-06-22 — v0.1 : exécution du chantier #2 (fork S-O2C-4, voie d'invention coin de transmission D↔N, étape NO-FETCH) sur GO Thierry, contre le gel anti-fit recouvrable 7cbf1072af03674062b07b585f13cca209179a2d851c71ab02ef341d08edba9f du cadrage LC-WORK-CADRAGE-O2-COIN-TRANSMISSION (cibles TC-G1..G4 + TC-R figées AVANT cette algèbre ; ordre gel→algèbre traçable). Fork S-O2C-4-2 = « KB-local/no-fetch d'abord (HOLD) » PRÉ-ENREGISTRÉ dans le cadrage gelé ⟹ l'étape no-fetch n'est PAS une décision de portée neuve ⟹ AUCUN amendement R-7. Appui : LMPS 1609.00207 (consommé ; backbone η=arcsinh(n·s), η-term nu) + LSW 2402.04308 (consommé ; famille conforme γ^p K fixé, p codim-1). RÉSULTAT par cible : TC-G1 forme DÉCOMPOSÉE S_TC=−(1/8πG)∮√σ[η+κ(p)·Φ] (gain vs COIN-b : décomposition backbone+correction, point dérivé = face D pince √σ au coin) MAIS κ(p)/Φ non dérivés no-fetch (terme de bord conforme explicite absent du KB) ; TC-G2 finitude z→0 NON franchie no-fetch (contre-terme codim-2 conforme + renorm FG absents), isolée comme gate ; TC-G3 sort de p GATÉ sur TC-G2 (p libre lean SAUF si finitude le sélectionne ; fixation de p ⟺ finitude ; non tranché), TC-G3★ p↔η indépendants dans le backbone (lien via Φ seul ⟹ fetch-gated, non tranché) ; TC-G4 AdS-only respecté (β≡G3 intact) ; TC-R m1 CONFIRMÉ (coin retiré ⟹ δC_mixte≠0), m2 MORD (κ→0 ⟹ Ξ→η ⟹ η-term nu de LMPS, non-tautologique), m3 non testable no-fetch. VÉRIFICATIONS SYMBOLIQUES (SymPy) STRUCTURELLES de backbone+m1/m2 = support de raisonnement, NON sceau de continuité (S-O2C-4-3 : aucune construction finie ⟹ délimitation SANS sceau, précédents C1-ADS/FACTORISATION/COIN). VERDICT = TC-b (délimitation, mieux situé) ⟹ PAS de TC-a ⟹ PAS d'armement de S-O2C-4 ⟹ PAS d'audit froid (S-O2C-4-4). APPORT (sans incrément de comptage) : surface inventive RESSERRÉE de « construire le coin » à « fournir κ(p), Φ, contre-terme de coin codim-2 conforme z→0 » ; fetch resserré nommé (priorité Witten ~1805.11559 ; Compère-Marolf ~0805.1902 ; Jubb et al ~1612.00149) = NOUVEL amendement R-7 daté requis. Registre : R-43 (face D Dirichlet pince √σ au coin mixte ⟹ δ√σ=0 sur B malgré face N conforme — point structurel dérivé no-fetch) ; R-44 (fixation de p ⟺ finitude renormalisée z→0 ⟹ TC-G3 gaté sur TC-G2, non tranchable sans le contre-terme conforme) ; R-45 (p codim-1 et η codim-2 indépendants dans le backbone ; lien p=f(η) ne passe que par Φ, fetch-gated — TC-G3★ non tranché). Fichier neuf (additif, .md +1, id +1). SANS SURCLASSEMENT (§6.4) : décomposer la forme ≠ dériver κ(p)/Φ ≠ finir au bord conforme ≠ fixer p ≠ construire α ≠ construire O₂ (β≡G3 reste) ≠ fermer D1 ; geler/exécuter une étape no-fetch ≠ résoudre ; {A4 ; A2★ ; N} INCHANGÉ ; N non fixé ; A4 non réduit ; A2★ non tranché ; CCC non démontrée. (cf. LC-WORK-CADRAGE-O2-COIN-TRANSMISSION, LC-WORK-BIBLIO-O2-COIN-TRANSMISSION, LC-D-O2-COIN, LC-D-O2-C1-ADS, LC-D-O2-FACTORISATION, LC-WORK-CARTOGRAPHIE-PRIORITES, 1609.00207, 2402.04308)"
prerequis_kb: [LC-WORK-CADRAGE-O2-COIN-TRANSMISSION, LC-WORK-BIBLIO-O2-COIN-TRANSMISSION, LC-D-O2-COIN, LC-D-O2-C1-ADS, LC-D-O2-FACTORISATION, LC-WORK-CARTOGRAPHIE-PRIORITES, 00_index, 03_glossaire]
source_externe: ["NO-FETCH (fork S-O2C-4-2 = HOLD honoré). Corps CONSOMMÉS (déjà en KB, jeu B2) : LMPS 1609.00207 (backbone η-term même-type : S_joint=−(1/8πG)∮η√γ, η=arcsinh(n·s), Eq. 2.89) ; LSW 2402.04308 (famille conforme [γ] et γ^p K fixés, p = poids GHY codim-1, Eq. 1.1). AUCUN fetch neuf. Candidats de fetch RESSERRÉS (hors KB ; non consommés ; tout fetch = nouvel amendement R-7 daté) : Witten ~1805.11559 (terme de bord conforme/Neumann → κ(p), Φ) ; Compère-Marolf ~0805.1902 (arête/codim-2) ; Jubb-Samuel-Sorkin-Surya ~1612.00149 (taxonomie coin à type mixte)."]
tags_epistemiques: [verdict, délimitation, à inventer, fetch-gated, C1-b, formalisable, mieux-situé]
gel_cadrage: "7cbf1072af03674062b07b585f13cca209179a2d851c71ab02ef341d08edba9f"
---

# Chantier #2 (facteur α) — voie d'invention du coin de transmission D↔N (fork `S-O2C-4`) : VERDICT

> **Verdict : `TC-b` — délimitation, mieux situé.** Première **décomposition structurelle** du coin de
> transmission :
> $$S_\text{TC} = -\tfrac{1}{8\pi G}\oint_B \sqrt{\sigma}\,\big[\,\eta + \kappa(p)\,\Phi\,\big]\,d^2\theta$$
> backbone **LMPS** (`η`) + **correction conforme** (`κ(p)·Φ`). Le firewall **m2 mord** (limite same-type →
> η-term nu de LMPS). Mais `κ(p)`, `Φ` et la **finitude `z→0`** ne sont **pas KB-dérivables** ⟹ **pas de
> `TC-a`** ; `α` reste **`C1-b`**. **AUCUN sceau** (aucune construction finie). **NO-FETCH** (aucun
> amendement R-7).

## 0. Anti-fit — ordre gel → algèbre (traçable)

Cibles **TC-G1..G4 + TC-R** figées dans `LC-WORK-CADRAGE-O2-COIN-TRANSMISSION` (gel recouvrable
`sha256 = 7cbf1072…dba9f`) **AVANT** cette algèbre. Le fork **S-O2C-4-2 = « no-fetch d'abord (HOLD) »** était
**pré-enregistré** dans le cadrage gelé ⟹ l'étape no-fetch **n'est PAS une décision de portée neuve** ⟹
**aucun amendement R-7**. (Un fetch des candidats resserrés déclencherait, lui, un amendement daté.) Appui
**KB-consommé** : LMPS `1609.00207` (backbone) + LSW `2402.04308` (famille `p`).

## 1. TC-G1 — FORME : décomposition structurelle (avance vs `COIN-b`)

Le coin de transmission `B` (codim-2) sépare la face **D** (éon `n`, GHY/Dirichlet, `g₀` fixé) de la face
**N** (éon `n+1`, Neumann/conforme, `g₃=0`). La variation de joint y est **désaccordée** (données conjuguées
différentes de part et d'autre). Structure obtenue :

$$S_\text{TC} = -\tfrac{1}{8\pi G}\oint_B \sqrt{\sigma}\,\big[\,\underbrace{\eta}_{\text{backbone LMPS}} + \underbrace{\kappa(p)\,\Phi}_{\text{correction conforme}}\,\big]\,d^2\theta, \qquad \eta = \operatorname{arcsinh}(n\cdot s).$$

- **`η`** = épine dorsale LMPS (consommé `1609.00207`, Eq. 2.89), **géométrique**, identique des deux côtés.
- **`κ(p)·Φ`** = correction de la face conforme : poids `κ` **hérité du terme GHY-`p`** de la famille LSW
  (`γ^p K` fixé, donnée codim-1), `Φ` = pièce **conjuguée** que le terme de bord conforme injecte à l'arête.
- **Point dérivé NO-FETCH (R-43).** Comme `B ⊂ D ∩ N` et que la face **D** est Dirichlet, elle **pince
  `√σ` au coin** : `δ√σ = 0` sur `B` **même quand la face N est conforme**. Donc
  `δ(η√σ) = √σ\,δη + η\,δ√σ → √σ\,δη` : la variation du joint se **réduit à la variation d'angle** — le
  mécanisme d'annulation du η-term survit au passage à la face conforme. *(Vérifié symboliquement.)*

**Mur no-fetch.** `κ(p)` et `Φ` **exigent le terme de bord conforme explicite** (et son IBP d'arête), **absent
des 5 corps KB** (FH-II omet le coin ; LMPS est GHY/Dirichlet des deux côtés ; LSW n'a pas de coin ; Skenderis
et de Haro sont codim-1). **TC-G1 : forme DÉCOMPOSÉE (backbone + correction), PAS dérivée** (`κ(p)/Φ` ouverts).

## 2. TC-G2 — FINITUDE `z→0` : non franchie no-fetch (le gate)

Le coin gît au **bord conforme** (`z→0`, FG), où `√σ` diverge (`~ z^{−(d−1)}`). La finitude renormalisée exige
un **contre-terme de coin codim-2 conforme** (au-delà des contre-termes codim-1 GHY/Robin) et le contrôle d'une
**anomalie de coin conforme** éventuelle. Le backbone `η` est fini en bulk, mais **rien dans le KB ne
renormalise le coin** au bord conforme. **TC-G2 : INDÉTERMINÉE no-fetch** — désormais **isolée comme LE gate**
de la construction (et, via §3, du sort de `p`).

## 3. TC-G3 — SORT de `p` (PORTANTE) : gaté sur TC-G2

Pour chaque `p`, le coin `S_TC` est ajouté pour **annuler la variation résiduelle mixte** `δC_mixte`. La
question est : cette bonne-position **fixe-t-elle `p`** ?

- **Lean = `p` libre (`C1-b`).** Le backbone `η` est géométrique et **indépendant** du poids `p` (confirme
  `COIN-b`/R-40). Tant que la correction `κ(p)·Φ` admet, **pour chaque `p`**, un coin **fini** qui annule
  `δC_mixte`, `p` reste **libre**.
- **Bascule possible (R-44) : fixation de `p` ⟺ finitude.** `p` ne serait **fixé** (`C1-a`) que si la
  **finitude renormalisée `z→0` (TC-G2)** échouait pour tout `p` sauf valeur(s) spéciale(s) — ou
  **sur-contraint** (`C1-c`) si elle échouait partout. Donc **le sort de `p` est gaté sur TC-G2** ⟹
  **non tranchable no-fetch** (le contre-terme conforme manque). **TC-G3 : `p` libre (lean), sort définitif
  GATÉ sur la finitude.**

### 3★. TC-G3★ — lien `p ↔ η` : indépendants dans le backbone (R-45)

`p` est une donnée **codim-1** (poids LSW sur les faces) ; `η` est un angle **codim-2** (géométrique, au coin).
Dans le **backbone**, ce sont **deux degrés de liberté distincts**. Un lien structurel `p = f(η)` ne pourrait
émerger **que** par l'intermédiaire de `Φ` (la correction conforme), **fetch-gated**. **TC-G3★ : NON tranché**
no-fetch ; **lean = indépendants**.

## 4. TC-G4 — PORTÉE : AdS-only respecté

Aucun transport dS tenté. Tout le raisonnement est en AdS/euclidien (LMPS WdW, LSW conforme AdS). **`β ≡ G3`
(transport dS du coin) INTACT** — chantier #1, hors scope. **TC-G4 : AdS-only ✓.**

## 5. TC-R — FIREWALL (structurel, sans sceau de continuité)

- **m1 (coin retiré) — CONFIRMÉ.** Sans `Ξ`, `δC_mixte = δη + κ(p)\,δΦ ≠ 0` génériquement ⟹ la variation
  codim-2 résiduelle / mal-position **réapparaît**. *(En limite same-type `κ→0` : `δC_mixte → δη` = résiduel
  LMPS, cancellé par le η-term — cohérent avec m2.)*
- **m2 (faces de même type) — MORD.** Face N → Dirichlet ⟹ la famille LSW collapse ⟹ la correction conforme
  **s'éteint** (`κ→0`) ⟹ `Ξ = η + κΦ → η` ⟹ `S_TC → −(1/8πG)∮ η√σ` = **η-term nu de LMPS**. Limite same-type
  **obligatoire tenue**. **Non-tautologique** : toute forme dont la correction **ne s'éteindrait pas** en
  Dirichlet **échouerait** ici.
- **m3 (orientation du boost / signe du conjugué) — NON testable no-fetch** : le signe de `Φ` requiert le
  terme de bord conforme explicite. *(Consigné, non levé.)*

> Les vérifications de §1/§5 sont **symboliques et STRUCTURELLES** (SymPy) — **support de raisonnement**, **non
> un sceau de continuité** : aucune **construction finie** n'a émergé (S-O2C-4-3), donc **délimitation SANS
> sceau** (précédents `C1-ADS`, `FACTORISATION`, `COIN`).

## 6. Verdict — `TC-b` (délimitation, mieux situé)

`TC-G1` (forme) **décomposée** mais `κ(p)/Φ` non dérivés ; `TC-G2` (finitude) **non franchie** ; `TC-G3`
(`p`) **gaté sur G2, non tranché**. Donc **`G1 ∧ G2 ∧ G3` non toutes satisfaites au sens fort** ⟹ **PAS de
`TC-a`** ⟹ **`TC-b`**. `α` reste **`C1-b`**. **Pas d'armement de `S-O2C-4`** (S-O2C-4-3) ⟹ **pas d'audit
froid** (S-O2C-4-4, armé seulement si `TC-a`).

## 7. Apport — surface inventive resserrée (sans incrément de comptage)

`COIN-b` listait trois sous-composantes manquantes (coin mixte ; finitude `z→0` ; le coin ne fixe pas `p`).
Le présent verdict les **resserre** en un objet de fetch **précis** :

1. **`κ(p)` et `Φ`** — le terme de bord conforme explicite + son IBP d'arête (la **correction** du backbone) ;
2. **le contre-terme de coin codim-2 conforme `z→0`** — qui décide **simultanément** la finitude (TC-G2) **et**
   le sort de `p` (TC-G3, via R-44).

**Fetch resserré nommé** (priorité) : **Witten `~1805.11559`** (bord conforme/Neumann → `κ(p)`, `Φ`) ;
**Compère-Marolf `~0805.1902`** (arête codim-2) ; **Jubb-Samuel-Sorkin-Surya `~1612.00149`** (taxonomie coin
mixte). **Tout fetch = NOUVEL amendement R-7 daté** (cf. `LC-WORK-BIBLIO-O2-COIN-TRANSMISSION` §4).

**Enjeu de falsifiabilité (aval, conditionnel, NON acquis).** Un futur `TC-a` (coin fini ∧ `p` fixé) pincerait
le coefficient O(1) de `A_T` en AdS ⟹ via la cartographie F5/O₂, convertirait le verrou W³ interne vers
**confrontable** — **mais** `TC-a` = `C1-a` **candidate**, `β≡G3` reste ouvert ⟹ **pas** D1 clos.

## 8. Registre

- **R-43** — au coin **mixte**, la face **D Dirichlet pince `√σ`** sur `B` (`B ⊂ D∩N`) ⟹ `δ√σ=0` **malgré** la
  face N conforme ; la variation du joint se réduit à `√σ·δη` (mécanisme d'annulation intact). *(Dérivé
  no-fetch.)*
- **R-44** — **fixation de `p` ⟺ finitude renormalisée `z→0`** : `TC-G3` est **gaté sur `TC-G2`** ; non
  tranchable sans le contre-terme de coin conforme.
- **R-45** — `p` (codim-1, poids LSW) et `η` (codim-2, géométrique) **indépendants dans le backbone** ; un lien
  `p=f(η)` (TC-G3★) ne passe que par `Φ` ⟹ fetch-gated, non tranché ; lean = indépendants.

## 9. Sans surclassement (§6.4)

**Décomposer la forme** (backbone + correction) **≠ dériver `κ(p)/Φ` ≠ finir au bord conforme ≠ fixer `p` ≠
construire `α` ≠ construire `O₂`** (`β ≡ G3` reste **ouvert** — chantier #1) **≠ fermer D1 ≠ falsifier/confirmer
CCC**. Geler puis exécuter une étape **no-fetch** ≠ résoudre. `{A4 ; A2★ ; N}` **INCHANGÉ** ; `α` reste
`C1-b` ; `β≡G3` ouvert ; D1 non clos ; N non fixé ; A4 non réduit ; A2★ non tranché ; CCC non démontrée.


## 10. Post-fetch resserré (2026-06-23) — `TC-b` CONSOLIDÉ

> **Incrément v0.2.** Fetch armé (amendement R-7 `4a3d0a71…`) des trois sources nommées, contre le **même
> gel** `7cbf1072…`. Le fetch **fait avancer** les cibles mais **ne produit aucune construction finie**
> ⟹ **pas de `TC-a`**, **aucun sceau**, **`α` reste `C1-b`**. Vérif structurelle `verif_O2_coin_transmission_fetch.py` **EXIT 0**.

### 10.1 Ce que le fetch fournit (par source)

- **Witten `~1805.11559`** — la BC **conforme** de la face N fixe la **classe conforme `[γ]` + la courbure
  moyenne `K`**, laissant **libre** la **courbure extrinsèque sans-trace `K̃`** (conjugué/réponse) ; problème
  **elliptique/bien posé**. ⟹ le **facteur conforme `ω` (échelle) n'est PAS fixé** (échangé contre `K`).
- **Compère–Marolf `~0805.1902`** — famille de BC à **métrique de bord dynamique** ; **contre-termes**
  rendant les fluctuations **normalisables** (finitude `z→0` par renormalisation) ; **famille à paramètre**
  = origine de **`κ(p)`** (interpole Dirichlet↔Neumann).
- **Jubb–Samuel–Sorkin–Surya `~1612.00149`** — pullback seul fixé ⟹ terme de coin = **angle de boost `η`**
  (confirme le **backbone LMPS** de la face D) ; **contre-termes de coin** requis quand le coin touche le bord asymptotique.

### 10.2 Gains par cible (gelée)

- **TC-G1 — DÉRIVÉE EN STRUCTURE** (vs « décomposée » en v0.1). Le résidu codim-2 de la face N est porté par
  l'**échelle `δω`** (puisque `[γ]` et `K` sont fixés, mais `ω` libre). Donc **`Φ` = conjugué de `δω`**
  (`∝ K̃`, Witten) et **`κ(p)` = poids de la famille York/Compère-Marolf**. Coefficient exact = **formalisable**
  (équation de bord explicite ; non extraite verbatim).
- **TC-G2 — FINITUDE CONDITIONNELLE** (vs « indéterminée »). Intégrande de coin `~ z^{-2}(η+κΦ)√σ₀`
  (**divergent nu**) ; le **contre-terme de coin conforme** soustrait `z^{-2}/z^0` ; **résidu = anomalie de
  coin conforme `A_c(p)·log z`**.
- **TC-G3 — gate AIGUISÉ.** La **bonne-position** `δC_mixte=0` **DÉTERMINE `S_TC` comme fonction de `p`** mais
  **NE FIXE PAS `p`** (soluble `∀ p`) ; **seule la finitude/anomalie** le sélecterait ⟹ **`p` fixé ⟺ `A_c(p)=0`
  a une solution isolée** (raffine `R-44`). **`TC-G3★`** : `p` (codim-1) et `η` (codim-2) restent indépendants
  hors `Φ`.
- **TC-G4** — AdS-only respecté (`β≡G3` intact). **TC-R** — **m1 MORD** (coin retiré ⟹ résidu `δω` réapparaît) ;
  **m2 MORD** (face N → Dirichlet ⟹ `δω=0` ⟹ `κ(p)·Φ→0` ⟹ `η`-term nu de LMPS) ; **m3** partiellement
  adressable (signe de `Φ` via `K̃`) mais test complet gaté sur `A_c`.

### 10.3 Nouveau mur (reshapé, nommé)

Le mur **n'est plus** « `κ(p)/Φ` absents » mais « **coefficient d'anomalie de coin conforme `A_c(p)` non
calculé** ». `A_c(p)` exige le **contre-terme de coin conforme explicite**, hors des trois sources armées —
candidats **NON armés** (à confirmer) : renormalisation EH avec contre-termes de coin (`~JHEP 05(2020)064`),
Brown-York à BC mixte (`~JHEP 11(2021)224`). Leur consommation = **nouvel amendement R-7 daté**.

### 10.4 Registre

- **R-46** — face N conforme fixe `[γ]+K`, laisse `K̃` libre ⟹ le résidu codim-2 est l'**échelle `δω`**
  (non le boost) ⟹ `Φ` = conjugué de `δω` ; `κ` = poids York/CM.
- **R-47** — la **bonne-position** `δC_mixte=0` détermine `S_TC(p)` mais **ne fixe pas `p`** ; sélecteur = finitude.
- **R-48** — finitude `z→0` : résidu = **anomalie de coin conforme `A_c(p)·log z`** ; `p` fixé ⟺ `A_c(p)=0`
  isolé ; `A_c` exige le contre-terme de coin conforme (**source non armée**) ⟹ nouveau mur.

### 10.5 Sans surclassement (§6.4) — v0.2

**Identifier `κ/Φ` et nommer l'anomalie ≠ calculer `A_c(p)` ≠ fixer `p` ≠ exhiber `TC-a` ≠ construire `α` ≠
construire `O₂`** (`β≡G3` reste ouvert) **≠ fermer D1**. `{A4 ; A2★ ; N}` **INCHANGÉ** ; `α` reste `C1-b` ;
D1 non clos ; N non fixé ; A4 non réduit ; A2★ non tranché ; CCC non démontrée.

## 11. Post-fetch `A_c` (2026-06-23) — `TC-b` CONSOLIDÉ MAXIMAL : gate fermé, `p` libre

> **Sources consommées** (amendement `LC-WORK-AMENDEMENT-R7-O2-COIN-AC`, sha recouvrable
> `1023a02d…9e51c`, gel `7cbf1072` intouché) : **Gustavsson `1911.04178`** (renorm. EH + contre-termes de
> coin ; anomalie conforme `S^k×H^{d−k}`) et **Odak-Speziale `2109.02883`** (Brown-York à BC mixte ; coins
> non-orthogonaux). Cibles `TC-G1..G4`/`TC-R` figées **avant** ce fetch.

### 11.1 `TC-G1` — forme PLEINEMENT DÉRIVÉE EN STRUCTURE

Odak-Speziale fournissent la famille exacte. Lagrangien de bord `ℓ^b = b·s·K·ε_Σ` (éq. 2.13) et de coin
`ℓ^c = c·β·ε_S` (éq. 2.16), avec la table

| BC | fixé au bord | `b` | fixé au coin | `c` |
|---|---|---|---|---|
| Dirichlet | `q_{μν}` | 2 | `γ` | 2 |
| York (mixte) | `(q̂_{μν}, K)` | 2/3 | `β` | 0 |
| Neumann | `Π̃^{μν}` | 0 | `β` | 0 |

⟹ **identifications fermées** : `p ≡ b` (poids GHY retenu) ; `η = c·β` où `β` = boost normal-normal
(`sinh β = n̄·n`, Hayward, éq. 2.6/2.15) = le backbone LMPS `arcsinh(n·s)` ; **`Φ = Π̄_⟨μν⟩`** = moment ADM
**sans-trace** (charge de York, éq. 3.17) = la pièce conjuguée sans-trace de Witten. La décomposition
`S_TC = −(1/8πG) ∮_B √σ [η + κ(p)·Φ]` est donc **dérivée en structure** (coefficients = fonctions explicites
de `b`).

### 11.2 `TC-G2` — FINITUDE `z→0` : **FRANCHIE** (le gate est fermé)

Deux cadres de renormalisation **indépendants** rendent l'action incluant le coin **finie** :

- **Gustavsson** : l'**anomalie conforme** (coeff. du log) est **INDÉPENDANTE de `k`** (la structure de coin) —
  démontré explicitement (d=2 : `A=−1/2G` pour `k=0,1,2`). ⟹ **le coin n'engendre AUCUNE anomalie conforme
  p-dépendante** ; pour `d` impair, terme fini (pas d'anomalie), aussi indép. de `k`. Le contre-terme de coin
  existe (`I_ct,corn`) et renormalise.
- **Odak-Speziale** : la charge quasi-locale incluant le coin diverge à l'infini mais est **renormalisée à finie
  `∀b`** (subtraction `ℓ_div`, §3.4 ; éq. 3.24/3.26). Aucune valeur de `b` n'est privilégiée par la finitude.

⟹ **l'hypothèse v0.2 `R-48` d'une anomalie de coin conforme `A_c(p)·log z` SÉLECTRICE de `p` est RÉFUTÉE** :
il n'existe pas d'anomalie de coin p-dépendante qui pourrait fixer `p`.

### 11.3 `TC-G3` — sort de `p` : **TRANCHÉ → `p` LIBRE** (`C1-b` positif)

Deux conditions tiennent **pour tout `p`** :

- **bonne-position** : le potentiel symplectique de coin `ϑ^EH` est le **MÊME `∀b`** (Odak-Speziale §3.2,
  textuel) ⟹ `δC_mixte=0` est satisfaite `∀p` ⟹ **ne fixe pas `p`** (confirme `R-47`) ;
- **finitude** : établie `∀p` (§11.2) ⟹ **ne sélectionne pas `p`**.

⟹ **aucun `p★` isolé** ⟹ **`C1-b` confirmé POSITIVEMENT** (plus un *lean* gaté, mais une conclusion). Le choix
de `p` = **choix de fermeture du système** (Odak-Speziale : « the notion of energy depends on how we close the
system » ; Kerr → `M`, `2M/3`, `M/2` pour `b=2,2/3,0`) — une **liberté physique réelle**, pas une indétermination
à lever. **`TC-G3★`** : `p` (poids `b`, codim-1) et `η` (boost `β`, codim-2) **indépendants** confirmé (`Φ=Π̄_⟨μν⟩`
et `β` sont des données géométriques distinctes au coin).

### 11.4 Firewall — concrétisé via la table `b/c`

- **m2** MORD : face N → Dirichlet ⟹ `b:p→2`, `c:0→2` ⟹ le terme de Hayward `2β` réapparaît, `κ(p)·Φ→0`
  (métrique fixée ⟹ `δω=0`) ⟹ **retour exact au `η`-term nu** de LMPS. Non-tautologique.
- **m1** MORD : coin retiré ⟹ la variation de coin `∓2δβ ε_S` (Odak-Speziale éq. 2.6) **n'est plus annulée**
  ⟹ mal-position réapparaît.
- **m3** : le signe de `Φ=Π̄_⟨μν⟩` est accessible (moment ADM), mais non discriminant ici (pas de sélection de `p`).

### 11.5 Verdict v0.3 — `TC-b` CONSOLIDÉ MAXIMAL

Le **gate `TC-G2` est fermé** (finitude `∀p`) et **`TC-G3` est résolu** (`p` libre, `C1-b` positif). Le facteur
de coin de transmission est une **famille à un paramètre `p`**, **PAS une construction unique** ⟹ **PAS de
`TC-a`** ⟹ **AUCUN sceau** (`S-O2C-4-3` non armé) ⟹ **PAS d'audit froid** (`S-O2C-4-4`). **`α` reste `C1-b`**,
désormais **classification FINALE** sur l'axe coin : **la voie d'invention « coin de transmission » est ÉPUISÉE
— elle ne réduit pas `α`**. Pivot indiqué : **`β≡G3` (#1)** ou **A4 (#3)**.

### 11.6 Registre

- **R-50** — Odak-Speziale identifie `p≡b` (éq. 2.13) et `η=c·β` (éq. 2.15/2.16) ; `Φ=Π̄_⟨μν⟩` = charge de
  York (éq. 3.17) ; `TC-G1` pleinement dérivée en structure.
- **R-51** — `TC-G2` FRANCHIE : anomalie conforme indép. de `k` (Gustavsson) + renormalisation finie `∀b`
  (Odak-Speziale) ⟹ aucune anomalie de coin p-dépendante ⟹ `R-48` (anomalie sélectrice) **RÉFUTÉE**.
- **R-52** — `TC-G3` TRANCHÉE → **`p` LIBRE** : bonne-position (`ϑ^EH` même `∀b`) ET finitude (`∀b`) `∀p`
  ⟹ aucun `p★` ⟹ `C1-b` FINAL ; **axe coin épuisé** ; pivot = `β≡G3` (#1) ou A4 (#3).

### 11.7 Sans surclassement (§6.4) — v0.3

**Franchir la finitude et trancher `p` libre ≠ fixer `p` ≠ exhiber `TC-a` ≠ construire `α`** (au contraire :
établir qu'il **n'existe pas** de `α`-de-coin **unique**) **≠ construire `O₂`** (`β≡G3` reste ouvert) **≠ fermer
D1**. `{A4 ; A2★ ; N}` **INCHANGÉ** ; `α` reste `C1-b` ; D1 non clos ; N non fixé ; A4 non réduit ; A2★ non
tranché ; CCC non démontrée.
