---
id: LC-D-O2-P2
titre: "Pivot O₂, étape P2 RE-CADRÉE — GATE ÉTAT : la sélection du mode propre +i (Bunch–Davies) est-elle une DÉRIVATION de la structure dS ou un CHOIX D'ÉTAT déguisé ? Relocalisée par (c) (LC-D-O2-HODGE = DISCORDANCE : la jonction ne porte pas le −1, qui vient uniquement de i^{d-1} dS source unique). VERDICT = DISCORDANCE / NÉGATIF PROPRE (G1-c ∧ G2-c), KB-local, fetch HOLD, sceau réel. Quatre appuis : (1) (α) EXCLUSION STRUCTURELLE — la dualité E↔B (S=[[0,−1],[1,0]], réelle, recoupée D²=−𝟙 NONLIN-2PT) exclut la famille α-vacua (Mottola–Allen) SANS intrant d'état : dét[v_α|S·v_α]=4μ s'annule ⟺ μ=0 (BD) ⟹ resserrement ∞→{BD,BD*} [jambe α = (i)] ; (2) (β) K-③ NON-CIRCULARITÉ — S RÉELLE commute avec la conjugaison, qui échange e₊↔e₋ : la paire propre est NON ORDONNÉE par S ⟹ le choix de RACINE +i n'est pas fourni par la structure ; il requiert une orientation EXTERNE (fréquence-positive η→−∞ / Hadamard) = condition POSÉE, ou A4 = CIRCULAIRE [jambe β = (ii) ⟹ G1-c] ; (3) NON-RELOCALISATION — le i^{d-1} GÉOMÉTRIQUE est confiné au préfacteur ℓ^{d-1} de C_T, le squelette de mode se transplante RÉEL (CT-DUAL-DS, « aucun i dans l'équation ») : les DEUX i sont SÉPARÉS, la « liaison analytique » qui relocaliserait +i depuis AdS n'est pas établie KB-local et l'évidence scellée la dément ⟹ G1-b écartée ; (4) (G2) RACCORD NON CONTRAIGNANT — la jonction D→N (HODGE, RÉELLE, sign-neutre) ÉCHANGE les racines (J·e₊∝e₋, J·e₋∝e₊ = BD↔BD*) ⟹ traite +i et −i symétriquement ⟹ n'impose pas +i (≠ G2-a) ; le seul sélecteur intrinsèque candidat (Hartle–Hawking sur S⁴ euclidienne) requiert dS EXACT, indisponible au crossover (jonction, dS brisé — F5/O₃) ⟹ COLLISION ①↔④ [G2-c]. RÉPONSE : +i n'est ni forcé par la structure dS (G1), ni contraint au raccord (G2) ; la sélection de +i est un CHOIX D'ÉTAT par cette voie, qui ne tiendrait, au mieux, que dans l'éon idéalisé (dS exact). (P1)∧(P2)⟹état=+i⟹WCH⟹A4 reste ouverte sur P2 comme P1 l'était (HODGE). FERME proprement la gate-état du pivot O₂, sans réouverture ni surclassement. FETCH HOLD (S-O2-P2-1 ii) : statut pinçable KB-local (F5 : sélecteur ∈ {Hadamard-posé ; A4} ; argument standard tombe par non-unitarité O₁), résidu non décisif (HH-S⁴ borné à l'éon idéalisé, capé par ①↔④). NOTE DE RAFFINEMENT (anti-fit) : K-③ scinde (α)/(β) ; le resserrement (α) est POSITIF (structure), mais la jambe DÉCISIVE (β) est NÉGATIVE — l'issue obtenue est la négative, donc PAS un fit. Sceau verif_O2_P2.py EXIT 0/28, firewall 3 mutations (m1 d / m2 signe Λ / m3 S²), réel (mord 3/3), orthogonal à l'état (sha256 f2b110e8508d). {A4 ; A2★ ; N} INCHANGÉ ; A4 non réduit ; D1 non clos ; N non fixé ; CCC non démontrée."
codename: LC-RACCORD
tags: [module-D, O2, P2, P2-recadre, gate-etat, etat, mode-propre, bunch-davies, plus-i, racine-pm-i, deux-i, i-puissance-d-moins-1, ds-source-unique, alpha-vacua, mottola-allen, hadamard, S4-euclidien, hartle-hawking, K3, non-circularite, non-relocalisation, dependance-ds, choix-detat, jonction, dirichlet-neumann, crossover, collision-1-4, G1c, G2c, discordance, negatif-propre, fetch-hold, firewall, branche-falsifiabilite, pivot, §6.4, A4, A2star, N]
type: "chaînon de front (branche FALSIFIABILITÉ, pivot O₂) — étape P2 re-cadrée : gate ÉTAT, SCELLÉ (verif_O2_P2.py). Subordonné à LC-WORK-CADRAGE-O2-P2 v0.1 (forme gelée AVANT algèbre) et à LC-AUDIT-VERDICT §6.4."
statut: "discordance (négatif propre) — P2 (gate état) FAIT (KB-local, fetch HOLD, sceau réel). Verdict G1-c ∧ G2-c : la sélection de +i n'est ni dérivée de la structure dS ni contrainte au raccord ⟹ choix d'état par cette voie. Ferme la gate-état du pivot O₂ comme (c)/HODGE a fermé la gate-jonction. NE réduit PAS A4, NE ferme PAS D1, NE fixe PAS N. Audit froid différable (négatif)."
statut_id: "validé après sceau (verif_O2_P2.py, EXIT 0, 28 asserts, firewall 3 mutations réelles, orthogonal à l'état, sha256 f2b110e8508d) — à enregistrer si validé."
version: 0.1
langue: fr
date: 2026-06-15
maj: "2026-06-15 — v0.1 : exécution de P2 re-cadrée (gate état du pivot O₂) sur cadrage gelé LC-WORK-CADRAGE-O2-P2 v0.1 (sha 1a95680b3432). KB-local d'abord ; fetch HOLD (statut pinçable via F5-ETAT-RACCORD). K-③ exécuté = K3-partial : (α) exclusion structurelle des α-vacua par la dualité E↔B SANS intrant [(i)] ; (β) choix de racine +i NON pincé par S (réelle, symétrie de conjugaison) ⟹ condition posée (Hadamard) ou A4 (circulaire) [(ii) ⟹ G1-c]. G1-b écartée (deux i séparés, liaison analytique non établie KB-local). G2 = collision ①↔④ (jonction réelle échange les racines ; HH-S⁴ requiert dS exact, indisponible au crossover) [G2-c]. Verdict composé (critère tripartite gelé) : (ii) ∨ collision ⟹ DISCORDANCE / NÉGATIF PROPRE. Sceau verif_O2_P2.py (EXIT 0/28, firewall 3, sha256 f2b110e8508d). Acquis amont NON re-dérivés : (b) O2-P1 swap s=+1 ; (c) O2-HODGE discordance ; CT-DUAL-DS S²=−𝟙/BD=+i ; WEYL-BUNCHDAVIES ⟨g₃⟩_BD=0 ; F5-ETAT-RACCORD arbre d'obstruction (O₁/O₃/R-26). SANS SURCLASSEMENT (§6.4) : {A4 ; A2★ ; N} INCHANGÉ ; D1 non clos ; N non fixé ; A4 non réduit ; CCC non démontrée."
prerequis_kb: [LC-WORK-CADRAGE-O2-P2, LC-D-O2-HODGE, LC-D-O2-P1, LC-D-O2-JONCTION, LC-D-CT-DUAL-DS, LC-D-CT-REALITE, LC-D-CT-DUAL, LC-D3-WEYL-BUNCHDAVIES, LC-D-F5-ETAT-RACCORD, LC-D-HOLOGRAPHIE-G3, LC-AUDIT-VERDICT]
renvois: [LC-D-O2-HODGE, LC-D-O2-P1, LC-D-O2-JONCTION, LC-WORK-CADRAGE-O2-P2, LC-D-CT-DUAL-DS, LC-D-CT-REALITE, LC-D3-WEYL-BUNCHDAVIES, LC-D-F5-ETAT-RACCORD, LC-WORK-BRANCHE-FALSIFIABILITE, LC-02-PROGRAMME, LC-03-GLOSSAIRE, LC-04-REFERENCES]
tags_epistemiques: [discordance, négatif propre, délimitation, établi, formalisable, à inventer, décision ouverte]
---

# LC-D · O₂ — P2 re-cadrée : dérivation dS vs choix d'état du mode propre +i (gate état)

> **Statut.** Étape **P2** (gate **état**) du pivot O₂, exécutée sur le cadrage **gelé**
> `LC-WORK-CADRAGE-O2-P2` v0.1 (anti-fit). **Verdict = DISCORDANCE / NÉGATIF PROPRE** (G1-c ∧ G2-c),
> KB-local, **fetch HOLD**, **sceau réel** (`verif_O2_P2.py`, EXIT 0/28, firewall 3 mutations, sha256
> `f2b110e8508d`). Ferme la **gate-état** comme (c)/HODGE a fermé la **gate-jonction**. **Ne réduit pas
> A4, ne ferme pas D1, ne fixe pas N.**

---

## 0. Rôle et garde-fou `[§6.4 + anti-fit + leçon (b′)/(c)]`

- **Étage.** P2 est **une** des deux gates de la réduction d'A4 (`LC-D-O2-JONCTION §5`). (c) a tranché
  **P1** (gate jonction) en **discordance** (HODGE) ; P2 (gate **état**) est ici tranchée. Ce qui est
  `établi (algèbre+structure)` = **le statut épistémique de la sélection de `+i`** (choix d'état, par
  cette voie), **jamais** la construction d'O₂ ni la réduction d'A4.
- **Non-surclassement (§6.4).** Une issue **positive** (P2-G1-a ∧ G2-a) n'aurait valu que « gate P2
  **dérivée** », **pas** A4 réduit. *A fortiori* le **négatif** ici ne touche **rien** : `{A4 ; A2★ ; N}`
  **INCHANGÉ** ; D1 non clos ; N non fixé (`≡ Λ`, `hors de portée`) ; CCC non démontrée.
- **Anti-fit.** Question P2, objet (les deux `i`), cibles **G1/G2** et issues pré-déclarées, critère de
  verdict **tripartite** : tous **gelés** dans le cadrage v0.1 (sha `1a95680b3432`) **avant** toute
  algèbre. **L'issue obtenue est la NÉGATIVE** (la racine n'est pas forcée) — l'inverse de l'issue
  agréable (`+i` dérivé rouvrant la chaîne vers A4). **Donc pas un fit** (cf. §3, note de raffinement).
- **Leçon (b′)→(c) appliquée.** Question **structurelle d'abord**, lecture du signe ensuite : on a établi
  **quelle opération réalise** (ou non) la sélection de `+i` **avant** d'en lire le statut.
- **Additivité / R-7.** Aucun chaînon amont scellé n'est touché (byte-identiques) ; patchs ultérieurs
  strictement additifs.

---

## 1. Rappel `[hérité — NE PAS refaire]`

- **(b) `LC-D-O2-P1`** : l'inversion de Penrose **swappe** source↔VEV (`g₀↔g₃`), `s=+1` **sign-neutre**.
- **(c) `LC-D-O2-HODGE`** : la jonction D→N **ne porte pas** le `−1` du Hodge ; le `−1` physique vient
  **uniquement** de la continuation dS `i^{d-1}|_{d=3}=−1` (**source unique indépendante**).
- **`LC-D-CT-DUAL-DS` (étape 2)** : sur les modes dS, `S²=−𝟙` (vp `±i`) ; `S` = dualité **E↔B** au niveau
  des EOM (`S(E)=B, S(B)=−E`), **réelle**, **indépendante de l'état**, recoupée `D²=−𝟙` (`NONLIN-2PT`,
  ab initio). `BD = f_a − i f_b ↔ (1,−i)` = vecteur propre `+i` ; `BD* ↔ (1,+i)` = propre `−i`.
- **`LC-D3-WEYL-BUNCHDAVIES`** : mode BD = positif-fréquence quand `η→−∞` ; carte FG `g₃=−iAk³/3`
  (radiatif libre, non local) ; `⟨g₃⟩_BD=0`, `⟨g₃g₃⟩~k³` ; `E_ij=(d/2H)g₃`.
- **`LC-D-F5-ETAT-RACCORD` (arbre d'obstruction)** : la famille **α-vacua** (Mottola–Allen) est **toute
  dS-invariante** ; l'argument standard « BD = unique état dS-invariant régulier le plus bas » repose sur
  la **positivité/le spectre** et **tombe** en dS/CFT **non unitaire** (O₁) ; la définition
  **euclidienne/Hartle–Hawking** de BD (régularité au passé infini) **= O₃ = la face holographique
  d'A4/WCH**, portée **au crossover par O₂** (R-26).

---

## 2. Le résultat de la structure `[P2 — sceau verif_O2_P2.py]`

**Le nerf : deux `i` distincts.** `i^{d-1}` **géométrique** (continuation `ℓ_AdS→iℓ_dS`, confiné au
préfacteur `ℓ^{d-1}` de `C_T`, `d=3→−1` réel, **indépendant de l'état**) vs `+i` du **mode propre**
(`f_a−i f_b`, l'**état**). La structure fournit le **couple `±i`** ; le passage `±i→+i` est l'endroit
précis où un choix d'état peut se déguiser.

**K-③ exécuté = K3-partial** (non-circularité). La sélection se scinde en deux jambes de statuts
distincts :

1. **(α) Exclusion STRUCTURELLE des α-vacua `[(i) sans intrant]`.** Famille `v_α = e₊ + μ e₋` (Mottola–
   Allen, `μ=e^α`, `BD ⟺ μ=0`). `dét[v_α | S·v_α] = 4μ` s'annule **⟺ μ=0** : un α-vacuum générique
   (`μ≠0`) **n'est pas** propre de `S`. La dualité **E↔B** exclut donc la famille **sans aucun intrant
   d'état** ⟹ resserrement `∞`(1-param) `→ {BD, BD*}`. **Résultat positif, non-circulaire.**

2. **(β) Choix de RACINE `+i` NON pincé par la structure `[(ii) condition posée ⟹ G1-c]`.** `S` est
   **réelle** ⟹ commute avec la conjugaison complexe, qui **échange** `e₊↔e₋` : la paire propre est
   **NON ORDONNÉE** par `S`. Le choix `+i` (BD plutôt que BD\*) **n'est pas fourni** par la dualité ; il
   requiert une **orientation externe** — fréquence-positive `η→−∞` / régularité Hadamard = **condition
   POSÉE** (intrant d'état), ou **A4** (le postulat à dériver = **circulaire**, interdit par K-③). Pas de
   sélecteur dS-intrinsèque en amont strict d'A4, **précisément** parce que la dS-invariance est **toute**
   la symétrie de fond et **ne suffit pas** (F5/O₁).

3. **Non-relocalisation — G1-b écartée.** Le `i^{d-1}` géométrique est confiné au **préfacteur de `C_T`** ;
   le squelette de mode se transplante **réel** (CT-DUAL-DS, « aucun `i` dans l'équation »). Les **deux
   `i` sont séparés** : la « liaison analytique » qui relocaliserait `+i` depuis le mode régulier AdS
   **n'est pas établie KB-local** et l'évidence scellée la **dément**. Sa vérification = le **fetch tenu en
   HOLD**, jugé **non décisif**.

4. **(G2) Raccord NON CONTRAIGNANT `[collision ①↔④ ⟹ G2-c]`.** Au niveau **structure du raccord**
   (S-O2-P2-3) : la jonction D→N (HODGE) est **réelle**, sign-neutre (vp `±1`) ; elle **échange** les
   racines `J·e₊∝e₋`, `J·e₋∝e₊` (**BD↔BD\***) ⟹ traite `+i` et `−i` **symétriquement** ⟹ **n'impose pas
   `+i`** (≠ G2-a). Le seul sélecteur intrinsèque candidat (**Hartle–Hawking sur `S⁴` euclidienne**)
   requiert **dS exact** ; le crossover **est** une jonction, **dS brisé** (F5/O₃) ⟹ `S⁴` **indisponible
   où `④` agit** ⟹ **collision ①↔④** : P2 ne tiendrait, au mieux, que dans **l'éon idéalisé**.

**Sceau.** `verif_O2_P2.py` — **EXIT 0, 28 assertions** ; firewall **K-⑤ 3 mutations** **réel (mord
3/3)**, **orthogonal à l'état** : **m1** `d` muté (`i^{d-1}|_{d=3}=−1` casse à `d≠3` ; conditionnalité
`d=3`) · **m2** signe `Λ` muté (facteur dS `−1` ≠ AdS `+1` ; dS-spécifique) · **m3** `S²=+𝟙` (vp réelles
`±1`, couple `±i` détruit, (β) s'évanouit). sha256 `f2b110e8508d`.

---

## 3. Verdict `[P2]`

**G1-c ∧ G2-c = DISCORDANCE / NÉGATIF PROPRE.** Critère composé gelé (cadrage §3) : `(ii)` **ou**
`collision ①↔④` ⟹ **discordance** — **les deux** tiennent. La sélection de `+i` est un **choix d'état**
par cette voie : **non dérivée** de la structure dS (G1), **non contrainte** au raccord (G2), son seul
candidat-dérivation (HH-`S⁴`) **indisponible** au crossover (①↔④). **La dérivation d'A4 par la gate-état
n'est pas établie.** La chaîne `(P1)∧(P2)⟹état=+i⟹WCH⟹A4` reste **ouverte sur P2** comme P1 l'était
(HODGE) : **les deux gates** du pivot O₂ ont livré un **négatif structurel**.

**Consolidations à consigner `[structure — PAS un mouvement de compte]` :**
- **(α)** la dualité **E↔B exclut structurellement** la famille α-vacua générique (`dét=4μ`, `∞→{BD,BD*}`)
  **sans intrant d'état** — resserrement réel de la cartographie de sélection ;
- **(G2)** le raccord (jonction réelle, sign-neutre) **échange** les racines BD↔BD\* — fait structurel
  propre du crossover, cohérent avec l'échange `g₀↔g₃=E↔B` de HODGE.

**Note de raffinement `[anti-fit]`.** K-③ scinde **(α)** (resserrement, **positif**) et **(β)** (racine,
**décisif**). Le libellé du cadrage autorisait l'issue agréable (`+i` dérivé), mais la dérivation donne
l'issue **négative** sur la jambe décisive (β) ⟹ **pas un fit**. Consigné, daté, non rétro-ajusté.

**Sans surclassement (§6.4) :** `{A4 ; A2★ ; N}` **INCHANGÉ** ; A4 non réduit ; D1 non clos ; N non fixé ;
CCC non démontrée.

---

## 4. Format de chaînon

- **Type** : résultat (scelle P2/gate-état du pivot O₂ ; un sceau). **Statut** :
  `discordance (négatif propre)` GIVEN intrants amont scellés ; verdict G1-c ∧ G2-c.
- **Sceau** : `verif_O2_P2.py` (EXIT 0, **28 asserts**, firewall **3 mutations réelles**, orthogonal à
  l'état, sha256 `f2b110e8508d`). Stack : Python 3.12 / SymPy 1.14. **Réexécutable, EXIT 0.** Rejoint la
  liste des sceaux de continuité.
- **Ne touche aucun chaînon existant** : algèbre `CT-DUAL-DS`, `CT-REALITE`, `O2-P1`, `O2-HODGE`,
  `WEYL-BUNCHDAVIES`, `F5-ETAT-RACCORD` intacte.
- **Fetch** : **HOLD** (S-O2-P2-1 ii). Statut pinçable KB-local (F5) ; résidu non décisif (HH-`S⁴` borné à
  l'éon idéalisé, capé par ①↔④). Décision et motif consignés ici.
- **Additivité** : `str_replace`, ancres `count==1`, suppressions limitées à `{version, maj}`.

---

## 5. Prochain pas `[handoff — sur GO]`

- **Pivot O₂ — les deux gates livrées NÉGATIVES.** P1 (jonction, HODGE = discordance) **et** P2 (état,
  ce chaînon = discordance). La réduction d'A4 par O₂ (chaîne `⟹WCH⟹A4`) **n'est pas ouverte** par les
  gates ; ce qui reste vers A4 est **principiel**.
- **Suite programme (rendement décroissant — décision Thierry)** : **F4** (A4 principiel, réserve de
  fond) ; **F6-G3** (flux-balance Λ-BMS, différé) ; le **résidu HH-`S⁴` en dS exact** (fetch HOLD,
  optionnel, non décisif — borné à l'éon idéalisé). `N ≡ Λ` `hors de portée`.
- **Audit froid (P2)** : **différable** (verdict négatif/discordance ; sceau réel, firewall mordant en
  place). **Menu des audits froids différés** (réancré) : `S-W3-4` (`LC-D-W3-GPY`) · `S-B5` (`LC-D-SPN`).
- **Propagation (lot additif)** : `LC-D-O2-JONCTION` (gate-état fermée, chaîne `⟹WCH⟹A4` non ouverte par
  O₂) · `LC-WORK-BRANCHE-FALSIFIABILITE` (fiche F5/O₂ : P2 discordance) · `03_glossaire` (entrées α-vacua /
  Mottola–Allen ; K-③ non-circularité ; collision ①↔④) · `04_references` (fetch HOLD non consommé) ·
  `LC-AUDIT-VERDICT` (bullet §8bis O₂ P2) · `00_index` (Carte + sceau).

---

## 6. Renvois, glossaire, références

**Renvois.** `LC-D-O2-HODGE` (c : gate-jonction discordance, relocalise P2) ; `LC-D-O2-P1 §2` (swap
`s=+1`) ; `LC-D-O2-JONCTION §5` ((C-O2), gates P1/P2) ; `LC-WORK-CADRAGE-O2-P2` (cible gelée) ;
`LC-D-CT-DUAL-DS` (S²=−𝟙, BD=+i, `i^{d-1}` confiné, garde-fou) ; `LC-D-CT-REALITE` (`i^{d-1}|_{d=3}=−1`,
source unique) ; `LC-D3-WEYL-BUNCHDAVIES` (mode BD, `⟨g₃⟩_BD=0`, `E∝g₃`) ; `LC-D-F5-ETAT-RACCORD`
(arbre O₁/O₃/R-26 : α-vacua dS-invariants, non-unitarité, BD-euclidien = A4) ; `LC-AUDIT-VERDICT §6.4`.

**Références (`LC-04`).** S-O2-P2-1 = KB-local d'abord (de Haro `0808.2054`, FG `d=3` ; `CT-DUAL-DS` ;
`WEYL-BUNCHDAVIES` ; `F5-ETAT-RACCORD`). Source `[ANTI-FETCH / post-gel, P2]` (critère d'exclusion
intrinsèque des α-vacua : régularité euclidienne `S⁴` / Hadamard) **NON consommée** : fetch **HOLD**
(statut pinçable KB-local, résidu non décisif).

**Glossaire (`LC-03`) — à ajouter si validé.**
- *α-vacua (Mottola–Allen)* : famille à 1 paramètre `μ=e^α` d'états dS-invariants, `BD ⟺ μ=0` ; la
  dS-invariance seule les laisse libres.
- *K-③ (non-circularité)* : le mécanisme de sélection doit être en amont strict d'A4, sans `g₃→0` ni
  BD-comme-état. K3-partial pour P2 : (α) propre, (β) circulaire/posé.
- *Collision ①↔④* : le sélecteur intrinsèque candidat (HH-`S⁴`) requiert dS exact, indisponible au
  crossover qui brise dS ⟹ P2 ne tiendrait qu'en éon idéalisé.

---

## Appendice — Légende des tags épistémiques

`discordance` / `négatif propre` : la sélection de `+i` n'est ni dérivée (G1) ni contrainte (G2) par
cette voie — résultat **scellé**, ferme la gate-état sans surclassement.
`délimitation` : le statut (choix d'état) est consigné ; la chaîne `⟹WCH⟹A4` reste ouverte.
`établi` : ici, l'algèbre de structure (S²=−𝟙, exclusion `dét=4μ`, échange J des racines, firewall) —
`établi (algèbre+structure)`, **jamais** « A4 réduit / D1 clos / CCC démontrée ».
`formalisable` : la grille tripartite (α/β + raccord) appliquée KB-local.
`à inventer` : O₂ (construction) ; la chaîne `⟹WCH⟹A4` ; un éventuel sélecteur `+i` intrinsèque
disponible **au crossover** (n'existe pas KB-local).
`décision ouverte` : le résidu HH-`S⁴` en dS exact (fetch HOLD) — non décisif.
**A4 non réduit ; D1 non clos ; {A4 ; A2★ ; N} INCHANGÉ ; N non fixé ; CCC non démontrée.**
