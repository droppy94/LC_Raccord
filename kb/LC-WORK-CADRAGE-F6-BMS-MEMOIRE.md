---
id: LC-WORK-CADRAGE-F6-BMS-MEMOIRE
titre: "Cadrage paper-first du front F6 (branche FALSIFIABILITÉ, rang 6 — BMS / mémoire gravitationnelle). Gèle AVANT tout fetch les cibles F6-G1..G4 (anti-fit) et acte les scopings S-F6-1..4 tranchés « tout recommandé » (décision Thierry) : périmètre (a)+(b) [structure asymptotique + réduction de datum G1/G2 ; volet Λ>0/ΛBMS G3 ; (c) confrontation observationnelle EXCLUE = décision séparée] ; sources KB-only d'abord, fetch externe conditionnel POST-gel pour G3 ; livrable hybride (sceau sur la part interne G1/G2, délimitation pour G3) ; audit froid différé/réversible. Issue honnête attendue = consolidation/délimitation + obstruction Λ nommée ; le périmètre ouvert irréductible {A4 ; A2★ ; N} est INCHANGÉ ; D1 non clos ; N non fixé ; CCC non démontrée. ACTIVE."
codename: LC-RACCORD
tags: [cadrage, paper-first, falsifiabilite, branche-transverse, F6, BMS, memoire-gravitationnelle, triangle-IR, soft-theoreme, supertranslation, cisaillement, news, Lambda-BMS, dS, crossover, Weyl-electrique-magnetique, CFT-celeste, anti-fit, gel, §6.4, ACTIVE]
statut: work-active
version: 0.1
langue: fr
maj: "2026-06-15 — v0.1 : création du cadrage F6 (paper-first). Cibles F6-G1..G4 GELÉES AVANT toute lecture de source et toute algèbre (anti-fit ; ordre gel→fetch tracé : ce fichier daté 2026-06-15 PRÉCÈDE toute consommation de source F6). Scopings S-F6-1..4 « tout recommandé » actés (décision Thierry). Adossé à la fiche F6 de LC-WORK-BRANCHE-FALSIFIABILITE v0.5 (objectif, ancrage, segments à étayer, issue honnête attendue = décision ouverte exploratoire). Aucun fetch, aucune algèbre, aucun sceau à ce stade. SANS SURCLASSEMENT (§6.4) : {A4 ; A2★ ; N} INCHANGÉ ; D1 non clos ; N non fixé ; CCC non démontrée."
---

# Cadrage F6 — BMS / mémoire gravitationnelle (paper-first)

> **Statut : GEL ANTI-FIT.** Ce fichier fige les cibles F6-G1..G4 **avant** toute lecture de
> source et toute algèbre. Toute décision de scoping *réductrice* postérieure au gel exige un
> **amendement formel et daté** des cibles (règle **R-7**), jamais une réinterprétation silencieuse.

## 0. Contexte et ancrage

Front **F6** de la branche FALSIFIABILITÉ (rang 6), adossé à la fiche F6 de
`LC-WORK-BRANCHE-FALSIFIABILITE` v0.5 : objectif = développer, depuis la CFT céleste / les
symétries asymptotiques, une **signature de mémoire gravitationnelle** (observable réel) ; item
ouvert touché = **prise empirique #2** de `02_programme` ; ancrage KB **faible** (le volet
BMS/mémoire n'est pas bâti) ; issue honnête attendue = **`décision ouverte exploratoire`**.

**Nuance d'ancrage (acquise, non spéculative).** Le *vocabulaire* BMS/mémoire est absent du KB,
mais la *donnée radiative sous-jacente* est déjà très développée et **scellée** :
- couple de Weyl rescalé au bord `𝓔 = (3/2H)·g₃` (électrique = ÉTAT) | `𝓑 = (1/H)·C[g₀]`
  (magnétique = GÉOMÉTRIE) — `LC-D-NONLIN-VERROU`, `LC-D-CB-WEYL-MAGNETIQUE` ;
- cisaillement / WCH au crossover (`LC-D3-WCH-GWE`, `LC-D3-SPECTRE-K3`, deux-point `⟨g₃g₃⟩~k³`) ;
- tenseur de stress céleste `C_T ~ N` et son secteur (`LC-D-CT-ATN`, `LC-D-CT-DUAL`, `LC-D-CT-REALITE`).

F6 n'est donc **pas** un volet *ex nihilo* : c'est une **traduction** de ces objets dans le langage
asymptotique — *mémoire* ↔ saut net de cisaillement `Δσ` ; *supertranslation* ↔ action sur la
classe conforme `g₀` ; *charge molle* ↔ secteur Goldstone de la CFT céleste.

## 1. Question F6

À la frontière conforme `𝓘⁺` (identifiée au crossover `𝒞`), la structure
soft / mémoire / symétrie-asymptotique fournit-elle :
- **(a)** une **réduction de datum** — la mémoire se ramène à la donnée radiative *déjà* présente
  `{g₀, g₃, cisaillement}`, **sans nouveau paramètre libre** — et/ou
- **(b)** une **prise falsifiable** — une signature de mémoire *spécifiquement CCC* —
- ou bute-t-elle sur l'**obstruction Λ>0** (le `𝓘⁺` du programme est dS / spacelike) et ne livre
  qu'une **délimitation** ?

**Le point dur, nommé d'emblée.** Le triangle IR canonique (Strominger : théorèmes mous = mémoire =
symétrie asymptotique) est bâti à `𝓘` **nul, Λ=0**. Le `𝓘⁺` du programme est **dS, spacelike
(Λ>0)** : le groupe et la mémoire pertinents sont la version **Λ-BMS** (non standard, débattue dans
la littérature). C'est là que vit le « difficile / volet à bâtir » de la fiche F6.

## 2. Cibles gelées AVANT tout fetch (anti-fit) — F6-G1..G4

> Ces cibles encodent l'**attente honnête** du programme (consolidation / délimitation + obstruction
> Λ, **zéro réduction du compte**). Posture anti-fit assumée : une signature CCC-spécifique surgissant
> *malgré* ce gel serait un positif réellement protégé ; une confirmation de l'obstruction est la
> délimitation attendue. Les cibles sont des **prédictions à éprouver**, pas des résultats.

- **F6-G1 — réduction de datum.** La mémoire `Δσ` (saut net de cisaillement à `𝓘⁺`) **n'est pas**
  un datum indépendant neuf : en variables programme elle est portée par la **même** donnée radiative
  que le résidu gaussien de D1 — la classe conforme `g₀` (par la supertranslation) et la *news*
  construite sur l'évolution du bord (liée à `g₃` / au cisaillement). *Attendu : aucun paramètre
  libre au-delà de `{cisaillement / g₀ / g₃}`.*

- **F6-G2 — charge ↔ stress céleste.** L'aspect de charge de supertranslation (la charge molle /
  Goldstone) appartient au secteur de la **CFT céleste déjà cartographiée** — le secteur `C_T` / ⟨T⟩.
  *Attendu : CONSOLIDATION (le secteur mou se rabat sur le tenseur de stress holographique existant),
  PAS une réduction de comptage.*

- **F6-G3 — obstruction Λ>0.** L'IR-triangle plat est une limite `Λ→0` ; en dS-`𝓘⁺` spacelike, le
  volet mémoire est la version **Λ-BMS**. *Attendu : GAP NOMMÉ ⟹ au mieux une délimitation ; la
  seule « signature CCC-spécifique » concevable serait l'écart `Λ>0` / l'imprint de crossover, **pas**
  une prédiction nette à l'ordre perturbatif.*

- **F6-G4 — contenu falsifiable.** Un observable mémoire existe (LISA / PTA, en principe), mais à
  l'ordre Einstein / perturbatif le programme prédit la **mémoire GR standard** (aucune déviation
  CCC). *Attendu : contenu falsifiable au mieux une **cohérence** (style F2 / D1c, « non exclu »),
  pas une signature positive ; `{A4 ; A2★ ; N}` INCHANGÉ.*

## 3. Scopings tranchés — S-F6-1..4 (« tout recommandé », décision Thierry 2026-06-15)

- **S-F6-1 — Périmètre : (a)+(b).** Structure asymptotique + réduction de datum (G1/G2, interne,
  sceau-able) **ET** volet Λ>0 / Λ-BMS (G3, l'obstruction qui *est* le résultat attendu). **(c)
  confrontation observationnelle EXCLUE** du présent front = décision séparée (risque
  d'`inconfrontable` / surclassement tant qu'aucune signature CCC-spécifique n'est dérivée —
  même garde-fou que F2/F5 : NE JAMAIS convertir une borne en contrainte programme sans dérivation
  des `(a,b,c)` propres).
- **S-F6-2 — Sources : KB-only d'abord, fetch conditionnel POST-gel.** (a) cœur G1/G2 en KB-only
  (`2503.24263` Meissner-Penrose déjà en KB ; couple `𝓔/𝓑` scellé ; de Haro `0808.2054`) ;
  (b) fetch externe **après ce gel** seulement si nécessaire pour G3 (triangle IR Strominger
  ~1703.05448 ; Λ-BMS Compère et al. ; mémoire Christodoulou / Thorne). Tout fetch sera tracé
  (gel→fetch), cibles non amendées sauf R-7.
- **S-F6-3 — Livrable : hybride.** Sceau `verif_F6_*.py` sur la part **interne dérivable** (mémoire
  ↔ cisaillement ↔ g₀/g₃ ; charge ↔ stress céleste : G1/G2) ; **chaînon de délimitation** (style
  F2/F3, possiblement sans sceau) pour le volet Λ-obstruction (G3).
- **S-F6-4 — Audit froid : différé / réversible** (cohérent avec les fronts récents). Décision
  d'armement ultérieure, sans coût sur le présent front.

## 4. Plan d'exécution (un fichier à la fois, GO explicites)

1. **(ce fichier)** gel du cadrage — *en validation.*
2. **Cœur interne G1/G2 (KB-only).** Traduire `Δσ`/news et la charge molle dans les variables
   `{g₀, g₃, 𝓔, 𝓑, C_T}` ; établir la réduction de datum (G1) et le rabattement charge↔stress
   céleste (G2). Sceau `verif_F6_memoire_cisaillement.py` (algèbre interne, firewall).
3. **Volet G3 (fetch conditionnel post-gel).** Confronter la structure Λ-BMS / IR-triangle à
   l'obstruction Λ>0 ; nommer le gap (délimitation). Chaînon-verdict.
4. **Bilan F6** + propagation (lot additif transverse) sur GO séparé.

À chaque étape : `count==1` avant tout `str_replace`, YAML revalidé, deltas contrôlés, sceaux
EXIT 0 (contenu vérifié) avant dépôt.

## 5. Garde-fous / sans surclassement (§6.4)

- F6 est **exploratoire** : issue honnête attendue = consolidation (G2) / délimitation (G3) /
  cohérence (G4). Aucune de ces issues ne réduit le compte `{A4 ; A2★ ; N}`.
- `N ≡ Λ` reste **explicitement hors de portée** (rappel fiche F6) : aucun effort n'y sera dépensé.
- Anti-fit : cibles gelées **ci-dessus, avant tout fetch** ; tout token-indice nommé dans un futur
  document aveugle = fit interne à retirer ; amendement de cible = R-7 daté.
- **Sans surclassement** : « mémoire = Δσ traduite » / « charge molle = secteur C_T » =
  cohérence de structure ≠ « D1 clos / N fixé / signature CCC établie / CCC démontrée » ;
  `{A4 ; A2★ ; N}` INCHANGÉ ; D1 non clos ; N non fixé ; CCC non démontrée.
