---
id: LC-WORK-CADRAGE-NACTION-ALPHA
titre: "Cadrage analytique (paper-first) de la ROUTE α du résidu N_action (R1 du pont constructif) : tester si le facteur N_action⁻¹ = 4 (qui ferait PASSER R1) est IDENTIQUEMENT le facteur de convention deHaro/programme `=4` déjà catalogué (LC-D-CT-DUAL §4), et non une coïncidence numérique. CŒUR DU CADRAGE = architecture ANTI-NUMÉROLOGIE : audit de degrés de liberté (objectif ZÉRO paramètre libre), surdétermination par deux sources holographiques indépendantes (de Haro 0808.2054 ; Bueno–Myers 1505.07842), ordre blind [B-décomp AVANT A-cible 4], firewall par perturbation de chaque sous-facteur PINNÉ (sans knob de récupération). VERDICT VISÉ si fermeture : convergence comme CONSOLIDATION (le résidu R1 se rabat sur le `=4` catalogué — PAS de réduction de {A4 ; A2★ ; N}, PAS de physique neuve), établi (algèbre) CONDITIONNEL (hérite de la conditionnalité de κ [NACTION] et du `=4` [DUAL, AdS]). CONDITION DE NO-GO explicite : si la décomposition définitionnelle ne FERME PAS sur 4 ⟹ route α échoue ⟹ bascule β (ab initio R-2). N'est PAS un chaînon ; aucune algèbre scellée, aucune touche KB. Subordonné à LC-AUDIT-VERDICT §6.4. Compte {A4 ; A2★ ; N} INCHANGÉ."
codename: LC-RACCORD
type: "note de cadrage (working) — pose l'architecture anti-numérologie de la route α AVANT tout sceau. N'est PAS un chaînon : aucune algèbre, aucun sceau déposé ici. Subordonné à LC-AUDIT-VERDICT §6.4."
version: 0.4
langue: fr
date: 2026-06-10
maj: "2026-06-27 — ERRATUM F-PRISMA (label biblio) : attribution auteur de 1505.07842 corrigée « Bueno–Myers–Witczak-Krempa »/« Bueno–Myers–WK » → « Bueno–Myers » (papier à 2 auteurs ; W. Witczak-Krempa co-signe le compagnon 1505.04804, non utilisé ici). id, datum OD2 éq.(3.47) C_{T,E}, algèbre, {A4 ; A2★ ; N} INCHANGÉS — correction de traçabilité seule ; trace NUANCE conservée au log PRISMA ; re-gel PKG-SHA v2.1 en clôture. | 2026-06-10 — v0.4 : CORRECTION (audit à froid, LC-AUDIT-LOG-NACTION-ALPHA v1.1 §AF). Les résultats actés en v0.3 (« f_Π=1 DÉRIVÉ … f_W=4 ÉMERGE de deux chaînes indépendantes … N_action=1/4 dérivé ⟹ CONVERGENCE/CONSOLIDATION ») sont SURCLASSÉS sur l'interprétation : f_W≡1/b_prog ⟹ N_action≡b_prog = magnitude M_Pl²/4 de F, UNIQUE porteur POSÉE (formalisable ; facteur conv. 2 réservé), NON dérivée de S^(2)_graviton. Restent établi (algèbre) : f_Π=1 (d=3, vérif. tautologique), C_T^dH=8c_W=ℓ²/κ²=éq.90, forme k³, κ=24/π² (étalon scalaire libre externe ⟹ OD = contrôle croisé RÉEL ; charge « κ ajusté » réfutée). N_action=1/4 ⟹ décision ouverte ; consolidation CONDITIONNELLE au verrou M_Pl²/4. Note erratum ajoutée au §5. {A4 ; A2★ ; N} inchangé ; D1 non clos ; CCC non démontrée. | 2026-06-10 — v0.3 : RÉSULTATS PAPER ACTÉS + périmètre α∪R-2 confirmé. (i) f_Π=1 DÉRIVÉ (vérif sympy, frame p=(0,0,κ)) : le projecteur de Haro éq. 119 (Appendix C.2) = PiTT du programme à l'identique (écarts nuls 81 comp., trace 2) ⟹ le facteur 4 n'est PAS dans Π ⟹ RENVOI ADDITIF À CORRIGER sur LC-D-CT-DUAL §4 (sa décomposition « norm. de Π + préfacteurs » : la part Π vaut 1, tout le 4 est dans les préfacteurs W↔⟨TT⟩). (ii) Chaîne de Haro vérifiée : éq. 61 W=(ℓ²/8κ²)∫h□^{3/2}h, éq. 63 ⟨T⟩=2δW/δh, éq. 90 ⟨TT⟩=(ℓ²/κ²)|p|³Π ⟹ C_T^{dH}=8c_W (8=2 quadratique × 2 [⟨T⟩=2δW] × 2 [⟨TT⟩=2δ⟨T⟩]). (iii) f_W = R-2 : C_T^{prog}=2c_W DÉRIVÉ de la prescription du programme (|Im F|/k³=M_Pl²/(4H²)=ℓ²/(32πG)=2c_W), donc f_W=8c_W/2c_W=4 ÉMERGE de deux chaînes indépendantes — PAS de division de deux nombres scellés (numérologie écartée). Le sceau verif_naction_alpha.py absorbe donc R-2 (map ⟨TT⟩∝F). | 2026-06-10 — v0.2 : DEUX DURCISSEMENTS demandés. (1) OD exige désormais une TROISIÈME source dHSS indépendante (§4, OD3) avec honnêteté explicite sur la corrélation résiduelle (toutes holographiques) + alignement de convention PAR source ; web-vérif au sceau (lie la dette biblio N1/R-3). (2) Calcul de f_Π RENDU EXPLICITE (§2bis) : recette tensorielle concrète (frame p=(0,0,κ), résolution du scalaire c reliant Π^deHaro et Π^prog), avec le constat-clé que le SPLIT f_Π·f_W=4 est DÉTERMINÉ par les définitions (non posé) — seul le PRODUIT est testé contre 4 ; cas admis f_Π=1 (si Π^deHaro = projecteur unité). DoF §3 et NO-GO §6 mis à jour en conséquence. | 2026-06-10 — v0.1 : ouverture du cadrage route α. Exécute la décision de fork (route α d'abord) prise après rejeu de verif_D_CT_constructif.py (R1 = DÉCISION OUVERTE, réduit à N_action ; PASS exige N_action=1/4). Vérification de facteurs préalable (paper) : N_action⁻¹ = dHSS_OP/prog_OP = deHaro_nue/prog_imp = 4 EXACTEMENT, car κ=24/π² (NACTION, impulsion→OP) s'applique aux deux côtés et se simplifie. Ce cadrage pose l'architecture anti-fit (audit DoF, surdétermination, firewall, ordre blind) et les conditions de NO-GO. AUCUN sceau, AUCUNE algèbre déposée ici."
statut: "CADRAGE — à valider AVANT d'écrire verif_*. Route α retenue. Architecture anti-numérologie posée (§3-§5). Conditions de NO-GO explicites (§6). Périmètre {A4 ; A2★ ; N} inchangé ; D1 non clos ; CCC non démontrée."
prerequis_kb: [verif_D_CT_constructif, LC-D-CT-DUAL, LC-D-CT-DUAL-DS, LC-AUDIT-LOG-NACTION-AVEUGLE, LC-D-CT-ATN, LC-WORK-REPRISE-CONSTRUCTIF-R1, LC-WORK-REPRISE-POST-NACTION, LC-WORK-CADRAGE-CONSTRUCTIF, LC-AUDIT-VERDICT, LC-00-INDEX]
fichiers_compagnons_kb: [verif_D_CT_constructif.py, verif_D_CT_dual.py, verif_naction_aveugle.py, verif_naction_firewall.py, verif_D_CT_ATN.py]
tags_epistemiques: [établi (algèbre), formalisable, à inventer, décision ouverte]
---

# Cadrage route α — N_action ?= (=4 de DUAL)⁻¹, architecture anti-numérologie

> **But.** R1 (sceau `verif_D_CT_constructif.py`) est en `DÉCISION OUVERTE` : il a réduit toute
> la question de convergence à un seul facteur `N_action` (dictionnaire `⟨T⟩=(d/16πG)g_(d)` +
> comptage de polarisation), avec PASS ⟺ `N_action = 1/4`. La **route α** teste l'hypothèse que
> ce `1/4` est **identiquement** l'inverse du facteur de convention deHaro/programme `=4` déjà
> scellé (en AdS) par `LC-D-CT-DUAL §4`. **Le risque que ce cadrage doit neutraliser** : que la
> dérivation devienne un *fit interne* qu'un sceau (V100) « confirmerait » tautologiquement.
> L'architecture ci-dessous est conçue pour que **passer le sceau soit réfutable et informatif**.

---

## 1. Acquis de la vérification de facteurs `[paper, non scellé]`

Trois conventions du **même** nombre pur `C_T/N` (d=3) + une valeur externe :

| objet | valeur `C_T/N` | provenance | libre ? |
|---|---|---|---|
| programme, **impulsion** | `1/(32π²)` | `CT-ATN [D]` (fonction d'onde, \|Im F\|) | **pinné** (scellé) |
| `κ` (impulsion→OP) | `24/π²` | `NACTION`, **étalonné à l'aveugle** (scalaire libre `A_sc=1/256`, `C_T^sc=3/32π²`) | **pinné** (scellé, blind) |
| programme, **OP** | `3/(4π⁴)` | `= κ · 1/(32π²)` | dérivé (0 intrant neuf) |
| dHSS Einstein-AdS₄, **OP** | `3/π⁴` | Bueno–Myers `1505.07842` (3.47) — **externe** | **pinné** (datum fetché) |
| de Haro **nue**, momentum | `1/(8π²)` | `DUAL §4` (éq. 90, `C_T=ℓ²/κ²`) — **externe** | **pinné** (datum fetché) |

**Identité constatée** (vérif sympy, slack nul) :

$$N_{\rm action}^{-1}=\frac{(C_T/N)^{\rm dHSS}_{\rm OP}}{(C_T/N)^{\rm prog}_{\rm OP}}
=\frac{(C_T/N)^{\rm deHaro}_{\rm imp}}{(C_T/N)^{\rm prog}_{\rm imp}}=4,$$

**parce que** `κ` multiplie les deux côtés (`dHSS_OP=κ·deHaro_nue` ET `prog_OP=κ·prog_imp`) et
**se simplifie** dans le rapport. Donc `N_action=1/4 = (=4)⁻¹` *numériquement*. **Ce n'est pas
encore une dérivation** : il faut établir que le `4` est le **même objet** que le `=4` de DUAL,
décomposé en sous-facteurs **indépendamment cloués**.

---

## 2. Le gap à fermer `[le contenu réel de route α]`

DUAL §4 décompose son `=4` en :  **`{normalisation de Π} × {préfacteurs W↔⟨TT⟩}`**.
Le sceau constructif définit `N_action` par : **`{norm. ⟨T⟩=(d/16πG)g} × {comptage polarisation}`**.
Ancre commune : de Haro éq. (1) `⟨T_{ij}⟩=(d ℓ²/16πG) g_{(3)ij}`.

Correspondance **candidate** (à dériver, pas à poser) :

| sous-facteur DUAL | sous-facteur N_action | objet physique commun |
|---|---|---|
| préfacteurs `W↔⟨TT⟩` | norm. `⟨T⟩=(d/16πG)g` | chaîne action → `⟨T⟩` → `⟨TT⟩` (dérivées fonctionnelles, `−2`/`+2` éq. 63) |
| normalisation de `Π` | comptage de polarisation | structure tensorielle / trace (`Σ_s ε^s ε^s=Π`, `Π:Π=2`) |

**Tâche de route α** : calculer chaque sous-facteur depuis sa **définition** (algèbre tensorielle
ou convention de dérivée fonctionnelle), **sans le choisir pour que le produit fasse 4**, puis
vérifier que le produit **tombe** sur 4.

> ⚠️ **Constat-clé (honnêteté).** À ce stade je **ne connais pas la décomposition** du 4 — seulement
> que le produit doit valoir 4. La répartition entre `f_Π` et `f_W` n'est **pas posée** : elle est
> **déterminée par les définitions** (§2bis). Le split lui-même peut être convention-dépendant ;
> seul le **produit** est l'objet physique invariant testé contre 4. Cas parfaitement admis :
> `f_Π = 1` (si la `Π` de de Haro est déjà le projecteur unité, trace 2) ⟹ tout le 4 vit alors
> dans `{f_W × comptage}`. C'est précisément ce que le calcul explicite ci-dessous doit **trancher**.

---

## 2bis. Calcul définitionnel explicite de `f_Π` et `f_W` `[recette ; ordre blind]`

> **`[RÉSULTATS ACTÉS — v0.3, vérif paper sympy ; intrants de Haro extraits verbatim de 0808.2054]`**
> - **`f_Π = 1` (dérivé).** Éq. 119 (Appendix C.2) : `Π^{dH}=½(Π_ik Π_jl + Π_il Π_jk − Π_ij Π_kl)`,
>   `Π_ij=δ_ij−p_ip_j/p²` ; le `½` porte aussi le terme de trace ⟹ `−½ Π_ij Π_kl` = exactement
>   `−1/(d−1)` du `PiTT` programme en `d=3`. Vérif frame `p=(0,0,κ)` : écarts nuls sur 81 comp.,
>   trace 2 des deux côtés ⟹ `c=1`, **`f_Π=1`**. ⟹ **le facteur 4 n'est PAS dans `Π`.**
>   `[À PROPAGER : renvoi additif corrigeant LC-D-CT-DUAL §4 — la part « norm. de Π » vaut 1 ;
>   tout le 4 est dans les préfacteurs W↔⟨TT⟩.]`
> - **Chaîne de Haro (vérifiée).** Éq. 61 `W=(ℓ²/8κ²)∫h□^{3/2}h` ; éq. 63 `⟨T⟩=2δW/δh` ;
>   éq. 90 `⟨TT⟩=(ℓ²/κ²)|p|³Π`. Chaîne symbolique : `C_T^{dH}=8c_W=ℓ²/κ²`, avec
>   **`8 = 2`(quadratique)`× 2`(⟨T⟩=2δW)`× 2`(⟨TT⟩=2δ⟨T⟩)**. (`c_W=ℓ²/8κ²=ℓ²/64πG`.)
> - **`f_W` = R-2 (le morceau à dériver).** `C_T^{prog}=2c_W` se **dérive** de la prescription du
>   programme : `|Im F|/k³ = M_Pl²/(4H²) = ℓ²/(32πG) = 2c_W` (`F=(M_Pl²/4)a²(H'/H)`, `verif_naction_*`).
>   ⟹ **`f_W = 8c_W / 2c_W = 4` ÉMERGE** de deux chaînes **indépendantes** (de Haro éq. 63 ;
>   programme `Ψ→|Im F|`), **pas** d'une division de deux nombres scellés. Le sceau **absorbe R-2**
>   (map opérateur `⟨TT⟩∝F` rendue explicite).


**(a) `f_Π` = ratio de normalisation des deux structures tensorielles.** On exprime le *même*
`⟨T_{ij}T_{kl}⟩` momentum dans les deux bases et on lit le rapport des coefficients.

1. Frame fixe : `p=(0,0,κ)`, `κ>0` (même `sub0` que `verif_naction_*`). Sous-espace TT = 2-dim
   transverse à `p` (plan `x,y`).
2. **Structure programme** `Π^prog_{ijkl} = Σ_s ε^s_{ij} ε^s_{kl}` — **déjà scellée**
   (`verif_naction_firewall.py [A]` : `ε^1=diag(1,−1,0)/√2`-type, `ε^2` off-diag ; sans trace,
   transverses, orthonormés ; `Π:Π=2`, idempotent). Tenseur 3×3×3×3 **explicite**, zéro liberté.
3. **Structure de Haro** `Π^{dH}_{ijkl}` — lue **verbatim** dans 0808.2054 (éq. du `⟨TT⟩=C_T|p|³Π`,
   au voisinage de l'éq. 90 ; la **définition** de son `Π`, pas une reconstruction). À insérer
   comme tenseur explicite au sceau (P4, web-vérifié).
4. **Résolution** : trouver le scalaire `c` tel que `Π^{dH}_{ijkl} = c · Π^prog_{ijkl}`
   composante par composante (surdéterminé : 81 composantes, 1 inconnue ⟹ si pas de `c` unique,
   les deux `Π` ne sont **pas** proportionnels ⟹ **NO-GO de structure**, à reporter). Alors
   `f_Π := 1/c` (le coeff de `Π^prog` qui reproduit le `⟨TT⟩` écrit en `Π^{dH}`).
5. **Contrainte de cohérence (test gratuit)** : si `Π^{dH}` et `Π^prog` sont **tous deux** des
   projecteurs idempotents sur le **même** sous-espace TR-TT 2-dim, alors `Π:Π=2` pour les deux
   ⟹ `c=1` ⟹ **`f_Π=1`**. Le calcul de l'étape 4 le **vérifie** plutôt que de le supposer :
   un `c≠1` signalerait que de Haro normalise sa structure autrement (préfacteur non-idempotent).

**(b) `f_W` = préfacteur de la chaîne `W → ⟨T⟩ → ⟨TT⟩`.** Convention de dérivée fonctionnelle,
**définitionnelle** (de Haro éq. 63, déjà citée dans `DUAL [B]`) :

$$\langle T_{ij}\rangle = 2\,\frac{\delta W}{\delta h^{ij}},\qquad
\langle T_{ij}T_{kl}\rangle = 2\,\frac{\delta \langle T_{ij}\rangle}{\delta h^{kl}}
= 4\,\frac{\delta^2 W}{\delta h^{ij}\,\delta h^{kl}}.$$

`f_W` est le **rapport** entre le coefficient que le programme attribue à `⟨TT⟩` (issu de
`|F_fini|`, fonction d'onde `Ψ`, *sans* le `2δ/δh` de Haro — cf. `verif_naction_firewall [B]`,
`⟨TT⟩∝F` ancré sur le spectre) et celui de la voie de Haro `4 δ²W/δh²`. Calculé depuis la
**définition** des deux prescriptions, **avant** d'écrire le nombre 4. (C'est ici que peut loger
un facteur `2²=4` si la voie `Ψ` et la voie `2δW/δh` diffèrent par la convention de variation.)

**(c) Ce qui est testé** : `f_Π · f_W · (comptage pol.) =?= 4`. Le **split** entre les trois est
ce que (a)(b) **produisent** ; il n'est pas un degré de liberté (cf. §3). Si de Haro et programme
partagent `Π` unité (`f_Π=1`) et la même prescription `⟨TT⟩∝Ψ`, le 4 devra venir entièrement de
`f_W` (deux `2` de variation) — **ou la fermeture échoue** (NO-GO §6).

---

## 3. Audit de degrés de liberté `[le cœur anti-numérologie — objectif : ZÉRO paramètre libre]`

Pour chaque grandeur entrant dans le sceau, on exige une provenance qui **interdit l'ajustement** :

- `C_T/N` programme (impulsion) = `1/(32π²)` : **scellé** (`CT-ATN`), non touchable.
- `κ = 24/π²` : **scellé, étalonné à l'aveugle** sur le scalaire libre (NACTION) — *antérieur à R1
  et jamais ajusté sur sa cible*. C'est la garantie centrale : `κ` n'a **pas** été choisi pour
  faire passer R1.
- valeur dHSS `3/π³` (OP) : **fetchée** (3 sources), non choisie.
- valeur nue de Haro `1/(8π²)` : **fetchée** (éq. 90), non choisie.
- sous-facteurs `f_Π`, `f_W` (et `f_pol` si distinct) : calculés depuis **définitions
  tensorielles / convention de dérivée fonctionnelle** — **pas de knob**.

**Verrou supplémentaire (v0.2) : le SPLIT n'est pas libre non plus.** On ne choisit même pas
*comment* le 4 se répartit entre `f_Π`, `f_W`, `f_pol` : §2bis (a)(b) les **produit** chacun
depuis sa définition. Un fit exigerait au minimum la liberté de répartir 4 = a×b avec a libre ;
ici a et b sont tous deux pinnés ⟹ le produit est une **prédiction**, pas un ajustement.

**Bilan DoF (à matérialiser dans le sceau par un compteur explicite).**
- paramètres ajustables disponibles = **0** ;
- sorties appariées = **≥ 4** : (i) le nombre `4` lui-même ; (ii) la surdétermination §4 (**trois**
  sources holographiques concordent en OP après alignement de convention) ; (iii) la fermeture
  structurelle `Π^{dH}∝Π^prog` (§2bis a4) ; (iv) la discriminance firewall §5.
- `#sorties_appariées > #paramètres_libres (=0)` ⟹ **passer le sceau est informatif, pas un fit**.
  (Critère anti-numérologie du programme : règle dérivée, intrants libres strictement < sorties
  appariées, modèle réfutable — `LC-AUDIT-VERDICT §9` / RoB.)

---

## 4. Surdétermination `[TROIS sources holographiques — 0 intrant libre]`

**OD1.** de Haro nue `1/(8π²)` (0808.2054 éq. 90) continuée en OP via `κ` :
`κ · 1/(8π²) = (24/π²)/(8π²) = 3/π⁴`.

**OD2.** Bueno–Myers `1505.07842` (3.47) : `C_{T,E}=(3/π³)L²/G` (d=3) ⟹
`C_T/N = (3/π³)/π = 3/π⁴` en OP. **= OD1.**

**OD3 `[durcissement v0.2 — exigé]`.** Une **troisième** détermination, par une **forme générale-d**
(donc check d'un *type* différent : évaluer un closed-form à `d=3`, qui attrape une erreur de
transcription spécifique à `d=3`) :
`C_T = Γ(d+2) / [8 π^{(d+2)/2} (d−1) Γ(d/2)] · L^{d−1}/G`
(`1912.07035` (9.6) / `2202.10473` (4.9)) ⟹ à `d=3` : `Γ(5)/[8π^{5/2}·2·Γ(3/2)]·L²/G`.
À **évaluer symboliquement** au sceau et exiger `= 3/π³` (puis `/π → 3/π⁴` OP). Source candidate
de réserve : Larsen–Strominger `1405.1762` (même structure `I_{ij,kl}`, `d=3`).

**Les trois doivent coïncider en convention OP** : `3/π⁴`, *aucune* ne référençant le programme.
Le sceau **aligne explicitement la convention de chaque source** vers OP **avant** comparaison
(test que les cartes de convention sont correctes, pas seulement que les nombres collent).

> **Honnêteté sur l'indépendance (à inscrire dans le log).** Les trois sont **holographiques**
> (même `C_T` d'Einstein-AdS₄) ⟹ **corrélées** : OD3 durcit contre *mon* erreur de citation /
> transcription / alignement de convention, et contre une erreur `d`-spécifique (via le closed-form),
> **mais PAS** contre une erreur **universelle** de la valeur holographique dans la littérature
> (celle-là serait `hors de portée`). OD ≠ indépendance statistique ; c'est un **recoupement
> multi-source multi-convention**. Web-vérif des trois au sceau (P4) ⟹ clôt partiellement la dette
> biblio `N1`/`R-3` (`LC-WORK-REPRISE-POST-NACTION §3`).

*Si les trois ne coïncident pas* après alignement ⟹ erreur de convention/source en amont — **halte
et audit**, pas un fit à corriger.

**OD-struct.** L'identité `dHSS_OP/prog_OP = deHaro_nue/prog_imp` (le `κ` qui se simplifie) tient
**symboliquement** avant toute valeur numérique — structure, pas nombre choisi.

---

## 5. Firewall `[perturbation de chaque sous-facteur PINNÉ ; aucun knob de récupération]`

Le sceau devra **casser** sous chacune de ces injections, **sans** qu'un autre facteur puisse
compenser (puisqu'ils sont tous pinnés) :

1. `f_Π` faux (×2) ⟹ produit ≠ 4 ; `f_W`, `f_pol` pinnés ⟹ **pas de récupération**.
2. `f_W` faux (préfacteur `−2`→`−1`, ou signe) ⟹ produit ≠ 4.
3. comptage polarisation faux (`Π:Π`=1 ou 3 au lieu de 2) ⟹ produit ≠ 4.
4. valeur dHSS fausse (`5/π³` au lieu de `3/π³`) ⟹ **OD casse** (les trois sources divergent) **ET**
   facteur ≠ 4 — double détection.
5. `κ` faux ⟹ OD casse (mais NE change PAS le rapport `4` : contrôle que le `4` est bien
   `κ`-indépendant, cohérent avec §1).
6. `Π^{dH}` non proportionnel à `Π^prog` (composante perturbée) ⟹ étape §2bis a4 ne trouve **aucun**
   `c` unique ⟹ NO-GO de structure détecté (le test ne se contente pas d'un produit numérique).

Le test 5 est le **discriminant-clé anti-fit** : il prouve que le `4` n'est *pas* fabriqué par
`κ` (sinon on pourrait l'ajuster via `κ`), mais survit à toute valeur de `κ` — donc `κ` n'est pas
un knob caché.

---

## 6. Ordre blind + conditions de NO-GO `[réfutabilité]`

**Ordre imposé (mime l'anti-fit du sceau constructif `[B] avant [A]`).**
1. `[B]` calculer `f_Π`, `f_W`, `f_pol` depuis leurs **définitions seules** — *avant* d'écrire le
   nombre cible `4` ; figer la méthode.
2. `[A]` insérer les valeurs externes (dHSS, de Haro nue) **mécaniquement**.
3. `[C]` test : `f_Π·f_W·f_pol =?= 4` (slack nul) **et** OD1+OD2+OD3 coïncident en OP **et**
   `Π^{dH}∝Π^prog` (existence d'un `c` unique, §2bis a4).
4. `[D]` firewall §5 (6 injections).

**NO-GO explicites (route α PEUT échouer).**
- Si `f_Π·f_W·f_pol ≠ 4` depuis les définitions ⟹ le `=4` de DUAL **n'est pas** cette
  décomposition (caractérisation incomplète, ou résidu non-conventionnel) ⟹ route α
  **n'établit pas** la convergence ⟹ **bascule β** (ab initio R-2, `LC-WORK-REPRISE-POST-NACTION
  §3 R-2`). C'est un **résultat**, pas un échec à rattraper.
- Si `Π^{dH}` n'est **pas** proportionnel à `Π^prog` (pas de `c` unique, §2bis a4) ⟹ les deux
  structures tensorielles diffèrent réellement ⟹ le `4` n'est pas une simple renormalisation de
  `Π` ⟹ réexamen de la décomposition DUAL, possible bascule β.
- Si les **trois** sources OD ne coïncident pas après alignement ⟹ erreur amont (source/convention)
  ⟹ halte, audit (et signalement dette biblio).

---

## 7. Verdict visé + plafond §6.4 `[porté quoi qu'il arrive]`

**Si fermeture (`f_Π·f_W·f_pol = 4`, OD1 OK, firewall discriminant) :**
- `N_action = 1/4` **dérivé** (non posé) ⟹ R1 `[C]` PASSE ⟹ **CONVERGENCE**.
- **Mais verdict = CONSOLIDATION** : le résidu de R1 se **rabat** sur le `=4` déjà catalogué
  (`DUAL §4`) — même schéma que « réalité de `C_T` → item CFT de raccordement ». **PAS** de
  réduction du compte `{A4 ; A2★ ; N}` ; **PAS** de physique neuve.

> **[ERRATUM 2026-06-10 — audit à froid §AF.]** Le scénario « fermeture ⟹ CONSOLIDATION /
> `N_action=1/4` **dérivé** » ci-dessus est **SURCLASSÉ**. L'audit à froid (4 passes +
> ré-exécution, `b_prog` symbolique) établit `f_W≡1/b_prog` ⟹ `N_action≡b_prog` = la magnitude
> `M_Pl²/4` de `F`, **unique porteur POSÉE** (`formalisable` ; facteur de convention `2` réservé),
> **non dérivée** de `S^(2)_graviton=(M_Pl²/8)∫a³(γ̇)²`. La « surdétermination par deux chaînes
> indépendantes » se réduit à **une chaîne ancrée (de Haro) ÷ un nombre posé** ; le compteur
> `#libres=0` est faux (1 porteur ↔ 1 cible). ⟹ `N_action=1/4` = **`décision ouverte`** ;
> **consolidation CONDITIONNELLE** au verrou `M_Pl²/4`, pas une fermeture. **Restent `établi
> (algèbre)`** : `f_Π=1` (d=3), `C_T^dH=8c_W=ℓ²/κ²`, forme `k³`, et `κ=24/π²` calibré sur l'étalon
> scalaire libre **EXTERNE** ⟹ OD = **contrôle croisé réel** (la charge « `κ` ajusté / OD
> décorative » est **réfutée**). (cf. `LC-AUDIT-LOG-NACTION-ALPHA` §AF.)
- **`établi (algèbre) CONDITIONNEL`** : hérite de la conditionnalité de `κ` (NACTION, GRADE
  modéré-élevé, conditionnel à `Σεε=Π` et map `⟨TT⟩∝F`) et du `=4` (DUAL, algèbre **en AdS**).
- **Surclassement INTERDIT (§6.4)** : jamais « D1 fermé / `N` fixé / CCC démontrée ». Le `4` est
  **dérivé ou le verdict tombe** ; aucun rattrapage par convention.

**Ce que route α apporte concrètement** : le coefficient holographique externe du `C_T`
d'Einstein-AdS₄ (`3/π³`), passé par **deux cartes de convention indépendamment pinnées**
(`κ` aveugle ; `=4` définitionnel), **reproduit** la valeur scellée du programme. Cohérence de
coefficients sur-déterminée — rien de plus, rien de moins.

---

## 8. Livrable prévu (après validation de CE cadrage)

`verif_naction_alpha.py` (nom proposé) : blocs `[B]` décomposition définitionnelle — `f_Π` par
résolution tensorielle `Π^{dH}=c·Π^prog` (§2bis a), `f_W` par convention de variation (§2bis b),
comptage polarisation (`Π:Π=2`, scellé) — **sans fetch de la cible 4** ; `[A]` valeurs externes
(**trois** sources dHSS alignées en OP + de Haro nue) ; `[C]` test `f_Π·f_W·f_pol=?=4` + OD1·OD2·OD3
coïncidence + `Π^{dH}∝Π^prog` ; `[D]` firewall (**6** injections §5), + **compteur DoF explicite**
(`#libres==0`, `#sorties≥4`). Puis log d'audit (ISO 19011 / GRADE, dont honnêteté §4 sur la
corrélation des sources) et propagation additive — **dont renvoi additif corrigeant `LC-D-CT-DUAL §4`**
(la part « norm. de Π » du `=4` vaut 1 ; tout le `4` est dans les préfacteurs `W↔⟨TT⟩` ; le PRODUIT
`=4` de DUAL est inchangé). **Rien n'est scellé tant que ce cadrage n'est pas validé.**

---

## Appendice — Légende des tags épistémiques
`établi` / `formalisable` / `à inventer` / `hors de portée` / `décision ouverte`
(cf. `LC-00-INDEX`). Ici, en l'état : identité numérique `N_action⁻¹=4` **`établi (algèbre)`** ;
identification `N_action ↔ (=4 DUAL)` **`à dériver`** (objet de route α) ; verdict R1
**`décision ouverte`** jusqu'au sceau ; **D1 non clos** ; compte `{A4 ; A2★ ; N}` **inchangé**.
